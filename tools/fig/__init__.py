# -*- coding: utf-8 -*-
"""tools/fig — 論文用の図を**再現可能なスクリプト**で作る置き場。

規律
    - 図は必ず「読み込み関数(パス → データ dict)」と「描画関数(データ dict → Figure)」に
      分ける。テストは描画関数を**小さな合成データ**で呼ぶだけで済む(実資産に依存しない)。
    - 出力は PNG + SVG + **数値サイドカー JSON** の 3 点セット。JSON には図に出した数値を
      そのまま入れる(図を読まずに検算できる=親の検収の入口)。
    - 書き出す文字列に**絶対パス・個人名・ホスト名を入れない**(``_style.rel`` を通す)。
    - ``src/shibuya`` は読むだけ(``tools/c7`` と同じ作法で ``sys.path`` に足して import する)。

再現コマンドは ``docs/bench/figures/README.md`` に図ごとに書いてある。
"""
