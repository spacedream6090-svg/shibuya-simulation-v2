# CLAUDE.md — shibuya-simulation-v2 運用規律

> v1ではこの規律の大半がClaudeのプロジェクトメモリに依存していた。v2では**メモリが消えても規律が残る**よう本書に明文化する。メモリの引き継ぎ手順は [docs/handover-memory.md](docs/handover-memory.md)。

## 1. プロジェクト概要

現実の渋谷を仮想空間上に再現するLLM社会シミュレーション第2世代。第一目標=**世界そのものがプロダクト**(再現の実務形=現実整合アンカー台帳の成長+歪む場所の宣言/未来予測=アンサンブル)。正典: docs/design/v2-redesign.md(決定台帳)・v2-methodology.md(Phase 0-5)・v2_draft_proposal.md・docs/research/答申群。

## 2. 進め方の規律(ユーザーとの合意事項・恒常)

1. **Q&A対話式**: 要件はClaudeが問いを立てユーザーが答えて決める。全決定は仮=何度でも変更可。矛盾はClaudeが積極検出して確認。
2. **コード着手前の決定アジェンダ**: 実装前に決定項目(OPEN・build-vs-reuse・モデル選択)を提示し合意を得る。細部まで定義してから実装へ。**(2026-09-01強化・ユーザー指示)大枠までの合意で着手しない——シミュレーションに関するあらゆる項目の詳細までユーザーとClaudeの対話で計画を確定してから実装に入る(詳細設計ラウンドの地図はPENDING参照)。**
3. **拡張は先に聞く**: Claudeが有益と判断した修正・拡張も勝手に実装しない(設計者の指紋の最小化)。
4. **毎回事前リサーチ**: 各決定ラウンド前に先行研究・類似事例をWebで調べて提示。ユーザーの例示は出発点にすぎない——リサーチで候補を広げ重要度で選定。
5. **都度report**: タスク/ラン完了ごとに結果→含意→次の選択肢を短く報告し、分岐はユーザー判断を仰ぐ(まとめて後報告にしない)。

## 3. 世界観・設計ドクトリン

- **状態と集約はエンジン、意思決定と発話だけLLM**(v2の背骨・4答申が独立収束)。
- **世界の変化はエージェント行動の結果として実装**(棚が減る=店員の補充行動)。エンジン規則による代替はconf宣言つきフォールバック限定・カバー率を台帳へ。
- **自然界を模した仕組み志向**: トップダウン制度注入よりボトムアップ創発の素地を第一候補に。造語・組織形成は促進せず観察する。
- **ゲームは作らない**: ゲーム/VR産業(Epic・NVIDIA等)の技術知見を輸入する(常設リサーチレーン)。
- **DT定義**: 現実のある時点のスナップショットが舞台(リアルタイム同期は不要)。現実はデータ収集ツール。
- 憲法6原則・三層世界・認知3層は決定台帳を正とする。

## 4. 開発方法論(v2-methodology.md の要約・全て強制)

- **パターン台帳ゲート**: 全機能は「どの現実パターン照合で使うか」を1行で書けなければ作らない。
- 全cap/近似に `mechanism|expedient` タグ。expedientは感度試験で「結果を駆動していない」証明必須。
- 保存則系(経済等)は実装前に紙上で収支を閉じる。
- **性能予算(step時間/RSS)とバイト予算**を機能追加前に宣言しテストでゲート。逐次ループの新設は宣言必須。
- 較正データとholdoutは最初に分割・較正がholdoutに触れない。ablationはスケール前の完了条件。
- 検証ランは短く: pytest+mockが基本・実LLMは〜24stepスモークまで。フルランはユーザー明示時のみ。

## 5. 体制

- **Fable 5(親)= 計画・意思決定・検収・統合・コミット。実行役サブ = Opus 5(Agentツール model:"opus")**。
- サブエージェントは**コミット禁止**。サブの完了報告は信用せず、ディスク実在+親自身のpytestで検収。実データ規模の退化検査も。
- モデルの存在・IDは記憶で断言せずスキル/APIで確認。

## 6. 記録プロトコル

- **devlog**: 毎交換ごとに docs/log/devlog.md へ1エントリ追記(依頼/決定/実施/次)。10件たまったら devlog-compressed.md へ圧縮し0にリセット。冒頭に「N / 10」カウンタ。
- **台帳3ファイル**: STATUS.md(薄い索引)/IMPLEMENTED.md(概要+完了実装)/PENDING.md(未実装・判断待ち)。毎コミットで更新・完了項目はPENDINGから**消して**IMPLEMENTEDへ・ひと段落ごとに掃除パス。
- コミットメッセージは「第N 〜」形式(devlogエントリ番号と対応)+trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

## 7. セキュリティ(コミット前に必ず)

- **秘密スキャン**: `python tools/scan_secrets.py <対象ファイル...>` — **対象ファイルを引数で渡す**こと(引数なしは何も検査しない)。標準は `git diff --cached --name-only` の全ファイル。HITはexit 1=コミット中止。検出パターンはスクリプト内PATが正(本書には書かない=自己HIT回避)。
- APIキー(ODPT等)は**環境変数のみ**。リポにキー・サーバー名・IP・個人メールを書かない。
- `git add -A` 禁止。**明示パスのみステージ**。データ実体・秘密・スクラッチはgitignoreを解除しない。
- 論文リンクは正規ドメイン(arxiv.org・doi.org等)を安全確認してから記載。Webからのファイルダウンロード実行はしない(読むだけ)。
