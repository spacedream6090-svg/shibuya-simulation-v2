# 受入報告 C6(実 LLM スモーク+運用テスト)— 進行中(2026-09-09)

> 形式は build-report-C0〜C5 と同じ: 親(Fable)が自分で実行した出力のみを証拠とする。
> ブランチ `build/c5-c8`。C5 の出口(W17 ingest)と並行して C6-a/b を先行実装。

## 0. 構成

| 部分 | 内容 | 実装 | 状態 |
|---|---|---|---|
| C6-a | 艦隊接続クライアント(llm/fleet: FleetConfig/FleetClient/FleetBridge・§7 失敗の意味論・BN-4 順序制御・cache_salt・診断行)+engine/cli 結線(`--llm fleet --endpoints … --mode … --run-id … --tape … --fleet-wait-s`) | サブ O(Opus) | 実装完了・親検収=済(§1)・層2=未 |
| C6-b | 検証テスト T3/T5/T9(CPU)+実 LLM ハーネス(T6 bit 再現・T7 分布同値・指標 B 再測・ablation①・書式/役割語率) | サブ P(Opus) | 実装中 |
| C6-c | 2 行形パーサの許容(ラベル別名 目的地/目的/場所/対象物→対象・コメント/セリフ→ひと言 ほか・段0 辞書 探索系→移動/観察系→待機・`strict_format_ok`/`alias_used`/`dictionary_mapped` の二重指標) | サブ Q(Opus) | 実装完了(第1弾 172→第2弾 202 passed: 位置引数の回収「行動: 移動 なし なし」→対象/ひと言・辞書 v2 通勤/通学/進入→移動・`positional_used`/`dictionary_candidate`)・結線=サブ O |
| スモーク | 5,000 体×24 tick・実艦隊(7×Qwen3-8B INT8) | 親(サーバー ~/v2) | 第1回=済(§2) |

## 1. C6-a 親検収(09-09)

- `pytest tests/engine tests/llm tests/test_cli.py` 全合格(fleet 39+結線 8)・lint 5 kept・scan CLEAN。
- mock 経路の退化検査: `cli --agents 5000 --seed 1 --world data/world/v2` → checkpoint **fbad9b8119fcc287…**(結線前と同一=**艦隊結線単独では mock 経路は不変**)。退化検査ハッシュの履歴(mock 実資産 5,000 体×1 日・seed 1): ①**fbad9b8119fcc287…**/sessions 401(C5-b・艦隊結線単独では不変) → ②**eb7e07ccab05ec72…**/67(会話招待の設計準拠化: 両側 CONVERSING・0.8 抽選廃止・blake3 撹拌) → ③**8aaf3c7454f45cfe…**/57(層2 中-1/2 の修正: 承諾行を Phase C 前で消費・対象規則) → ④**d5d793375a9d322e…**/48(W17 週次表の ingest=計画境界が週次表へ・呼 49,847)。現行=④(親 ×2・層2 ×2 で一致)。
- 親が `--fleet-wait-s` を CLI に追加(スモークで艦隊の歩調に合わせるため・既定 0=純非ブロッキング)。

## 2. 実 LLM スモーク第1回(09-09 14:04・サーバー ~/v2・Python 3.10 venv・W17 生成と GPU 共用)

コマンド: `python -m shibuya.cli --agents 5000 --seed 1 --ticks 24 --world data/world/v2 --llm fleet --endpoints http://127.0.0.1:8200..8206 --model Qwen3-8B-int8 --mode smoke --run-id c6-smoke-1 --tape data/tape/c6-smoke-1 --fleet-wait-s 5`

```
壁時計 91.38s / 移動+密度 24.999 ms/tick(numba JIT の初回コンパイルが 24 tick に按分された値・1 日ランでは 0.27 ms)
LLM呼 833 = 0.17 呼/体/日(24 tick ぶん)
保存則 OK / checkpoint 1 点 75140932bd6fe4fa…
診断 deferred 4,393 / promoted 0 / degraded 833 / suppressed 373
診断 parse_error_rate 0.1765 / undefined_action_count 43 / tape_miss 0
[艦隊] 呼 833 / 帳尻 833 ・繰り延べ 0 ・書式再生成 154 ・TTFT p50/p99 0.43/0.81 s ・e2e p50/p99 1.75/3.66 s ・cache_salt 5edc5c7e…(smoke)
```

- 艦隊接続・録画(blocks/calls parquet 364 行)・保存則・呼の帳尻(憲法1)=**成立**。tokens_in 平均 1,644(min 1,302/max 2,828)・tokens_out 平均 41.2。
- **書式エラー率 0.176 > 0.10**(受入未達): テープの最終応答は全件 2 行形(再生成で回復)。初回失敗の原因=ラベルの言い換え(「目的地:」「目的:」)と語彙外の行動語(探索 20・観察 20・調査 4 ほか)。→ サブ Q(パーサ許容: 別名+段0 辞書)。行動分布: 待機 407・移動 197・乗車 105・購入 37・退去 30。
- degraded 833=24 tick の短いランでは起床の初期集中に対し tick 予算(≈35 呼)が小さく、下位 2 クラスが縮退実行される(1 日ランでは平準化)。
- **第2回(09-09 15:10・パーサ許容+二重指標・温度 0.7・max_tokens 96・run-id c6-smoke-2)**:
```
壁時計 85.10s / LLM呼 833 / 保存則 OK / checkpoint b346e037cf75cc55… / deferred 4,393 / degraded 833 / suppressed 405
書式エラー率 実効 0.040 / 厳密 0.101 (受入 ≤0.10) ・別名 0.062(52 呼) ・辞書写像 0.008(7 呼)
[艦隊] 帳尻 833/833 ・繰り延べ 0 ・TTFT p50/p99 0.43/0.77 s ・e2e p50/p99 1.79/3.51 s ・temp 0.7 max_tok 96 ・cache_salt ec63cead…
[艦隊書式] 温度0再生成 153(成功 118) ・未定義 9(engine 側 undefined_action_count は 0=計数点の差・層2 へ)
```
  - **受入(書式エラー率 ≤0.10)=実効 0.040 で PASS**(再生成後の残存・n=833≥59・温度 0.7)。厳密 0.101 は境界。**初回応答の失敗は 153/833=18.4%**(温度 0.7・実プロンプト 1,644 tok)で、その 118 件が温度 0 の再生成で回復=設計の失敗意味論(§7)どおり。初回率の改善はテンプレ v1.1(D-21・「対象:」固定の強調)で再測。定義の確定=D-28。
  - 未定義行動 9→段0 辞書写像 7 件・段1 以降 2 件(エンジン側では 0=計数の置き場が違う。層2 で照合)。

### 2b. 実 LLM 1 日ラン(09-09 15:35-16:04・c6-day-1・5,000 体×1,440 tick・7 レプリカ・温度 0.7・max_tokens 96・fleet_wait_s 1・W17 生成と GPU 共用)
```
壁時計 1,744.9 s / LLM呼 50,000 = 10.00 呼/体/日(L4 上限どおり)/ 保存則 OK / センサス 残差 0・ゲート PASS / checkpoint 4 点 7c8afcd3234a9474…
書式エラー率 実効 0.103 / 厳密 0.204 ・別名 0.105(5,230) ・辞書写像 0.001 ・温度0再生成 15,187(成功 10,027) ・未定義 81
診断 deferred 71,606 / promoted 145,979 / degraded 2,643 / suppressed 58,621 / conversation_sessions 0
[艦隊] 帳尻 50,000/50,000 ・繰り延べ 0 ・TTFT p50/p99 0.45/0.86 s ・e2e p50/p99 2.42/5.10 s
移動+密度 31.1 ms/tick(P2 ≤5 ms・mock 1 日は同サーバーで 0.27 ms)
```
- テープ解析(親): 行動分布 **購入 24,530(49%)**・移動 19,733・休憩 2,221・乗車 1,747・待機 1,591・退去 67・**会話 8**(対象=個体 ID 6・なし 2)。起床クラス: 会話 0=35,174・計画境界 1=12,183・個体 2=1,602・セル 3=1,041(=繰り延べ→昇格 S(e) で上位クラスへ上がった呼が多数)。tokens_out 平均 48.3・≥96 が 114。
- 所見: ①**書式エラー率 実効 0.103 が受入 0.10 に僅かに届かず**・初回失敗 30%=別名 10.5%+語彙外+その他(生応答のダンプ口をサブ O に依頼) ②**会話ゼロ**(mock 日は 401 セッション): 8B が 会話 を選ぶのは 8/50,000=モデル側の選好+対象解決の問題→切り分け中 ③**購入 49%**=8B の購買偏り(v1 の警告「LLM の向社会性/購買偏り」と同種)→歪む場所として宣言・指標 B/ablation で測る ④P2 31 ms/tick=切り分け中(fleet_wait の混入か経路探索の重さか) ⑤昇格 146k=L4 予算(10 呼/体/日)に対する実需要の超過(繰り延べアービタは設計どおり動作)。

### 2c. 切り分け(09-09 16:20・サブ O の計器+親のサーバー実測・c6-smoke-3=60 tick・2,083 呼)
- **書式の初回失敗の内訳**(`--fleet-debug-dir`・531 行): `missing_label:対象` 226・`対象+ひと言` 78・`ひと言` 6(=モデルが**ラベルを省いて値だけ並べる**「行動: 移動 なし なし」「行動: 待機 なし ひと言: なし」)・`unknown_action_word` 96(探索 50・通勤 16・観察 9)・別名 目的/目的地 125。→ 位置引数の許容(ラベル欠落時は 2 番目=対象・3 番目=ひと言)と同義語追加(通勤→移動)をパーサへ(サブ Q・実効率を下げる)。
- **P2(移動+密度)**: 壁 36.9 ms/tick に対し **CPU 2.8 ms/tick・待ち割合 0.92**=仕事は増えておらず、httpx の asyncio スレッドとの GIL 競合で壁時計が膨らむ(実装計画書 §4 が事前宣言した移行条件「httpx イベントループと計算の干渉→LLM クライアントを別プロセスへ」に該当)。40 万体の CPU 見積り ≈224 ms/tick(P2 5 ms は 5,000 体基準の壁時計=D-35 で物差しを確定)。
- **会話ゼロの原因**: テープ解析=会話を選んだ呼 8/50,000(0.02%・モデル選好)・同グループに他の呼 0。加えて `engine.commit.pair_partners` が LLM の対象欄を無視して同バッチ同セル最小 id を相手にする(C3 expedient)=行動契約書の対象スロット・知覚契約書 §6 起床(ii)被招待と不一致→**設計準拠(対象 ID で招待→次 tick 被招待起床)へ修正**(サブ O)。

### 2d. スモーク第4回(09-09 17:05・c6-smoke-4・24 tick・パーサ第2弾+会話招待+二重指標)
```
LLM呼 833 / 書式エラー率 実効 0.047 / 厳密 0.229 ・別名 0.053(44) ・位置読み 0.137(114) ・辞書写像 0.047(39) ・温度0再生成 2(成功 2) ・未定義 39(=段0 写像)
checkpoint 3a4cc3cdc1ec1793… / conversation_sessions 0(24 tick=深夜)
```
- **受入(書式エラー率 ≤0.10)=実効 0.047 PASS**。再生成は 153→2 に減少(位置読み・辞書候補で初回失敗を回収)。厳密 0.229=モデルの順守率の実態(テンプレ v1.1=D-21 の材料)。

### 2e. 実 LLM 1 日ラン第2回(09-09 17:57-18:27・c6-day-2・パーサ第2弾+会話招待(R の B6 提示前)・5 レプリカ)
```
壁時計 1,755.6 s / LLM呼 50,000 / 保存則 OK / センサス 残差 0 PASS / checkpoint 4e633518…
書式エラー率 実効 0.008 / 厳密 0.379 ・別名 0.088(4,381) ・位置読み 0.292(14,588) ・辞書写像 0.008(382) ・温度0再生成 106(成功 103) ・未定義 384(段0 写像)
診断 deferred 71,643 / promoted 146,230 / degraded 2,668 / suppressed 60,540 / conversation_sessions 0 / undefined_action_count 2
[艦隊] 帳尻 50,000/50,000 ・再送 1 ・TTFT p50/p99 0.50/0.86 s ・e2e p50/p99 1.25/1.94 s / 内訳 fleet_wait 999.8 ms/tick・llm 94.9・movement 30.8(壁)
役割語率 0.0(tools/c6/format_role_rates.py・docs/bench/c6_day2/)
```
- **受入(書式エラー率 実効 ≤0.10)=0.008 PASS**(1 日・n=50,000・温度 0.7)。厳密 0.379=モデルの順守率の実態(位置読みが 29%)。
- **役割語率 0.0**: 8B は 1 日で 補充/開閉店/発車/停車/指示 を一度も選ばない(通報 21 のみ)。役割行動は世界過程のフォールバック(conf 0.5)が担っている状態=D-38(テンプレ v1.1 での役割の提示強化 or 役割体の呼を T2 へ)。
- 会話セッション 0 は被招待の提示(サブ R)前の値。

### 2f. 被招待の提示(サブ R・09-09 18:40)
- 知覚契約書 §6 起床(ii)「被招待」を **B6 の起床行の値**(`wake_reason` スロット)として提示: 「P-1234があなたに話しかけました。応じるなら 行動: 会話 対象: P-1234、応じないなら別の行動を選びます。」(中立句を併記=誘導の緩和・ablation 第2陣候補)。**テンプレ SHA 161fe181 不変**(TEMPLATES/WAKE_REASON_TEXT に行を足さず定数は renderer 側)。B6 36→53 tok(最悪 79/80)。結線: `render(..., inviter)`・`LLMBridge.call(inviter)`・`engine/run._inviter_of`。tests/perception/test_b6_invite.py 11 本。
- **実 LLM 1 日ラン第3回(c6-day-3・09-09 18:45-19:15・B6 提示あり・5 レプリカ)**: 呼 50,000・帳尻・保存則 OK・センサス PASS・checkpoint 07b40a38…・書式 実効 0.007/厳密 0.385・**conversation_sessions 3**(第2回 0→3)。会話が少ないのは 8B が 会話 を選ばない(D-33)+TTL 切れ(D-36)。
- 効果: mock は原理的に測れない(MockLLM は本文を読まず対象「なし」)ため情報経路の A/B(同 LLM・実レンダラ vs stub): **招待 101→承諾 87/拒否 1** vs **stub 招待 147→承諾 0/拒否 147**。実 LLM の 1 日ラン(c6-day-3)で会話セッション数を再測中。
- 所見(R): 被招待が呼ばれる前に TTL(3 tick)切れ=5,000 体で 490/1,149=43%(D-36 ②)。

## 3. T3〜T9・指標 B・ablation①
- **T6 LLM 層 bit 再現(親実行・09-09 15:25・検証ラン構成 mode=calibration・温度 0.7・seed=call_id 由来・VLLM_BATCH_INVARIANT=1・W17 生成と GPU 共用=バッチ構成は毎回異なる)**: 同一 manifest で 2 回 → テープの応答 **833/833 が同一(1.0000)**・checkpoint も同一(b346e037cf75cc55…)。cache_salt 36de52ce…(calibration)。→ この機・この版(vLLM 0.28.0・A5000・8B INT8)では bit 再現を主張できる(B14 の自検証と整合)。
- **T7 LLM 層 分布同値(親実行・mode=production・温度 0.7・seed 1 vs 2・各 833 呼)**: 行動語分布の JSD **0.0035 bit**(待機 374/378・移動 226/217・乗車 108/108・購入 46/61・退去 34/34・休憩 33/22)。合格線は D-25(仮 ≤0.05)→合格。
- **T3/T5/T9・L4/L6(サブ P・CPU・親が全体 pytest で再実行)**: T3 スレッド数 1 vs 8 で final_hash 一致(合成 400 体・実データ 5,000 体・engine に prange なし)/T5 5,000 体×24 tick×3 seed: selected_count r=−0.017・mean_wake_tick r=+0.012(|r|≤0.05)・母集団反転 KS p=1.000・JSD 0.00054/T9 診断 4 列(繰り延べ/昇格/縮退/抑止)非ゼロ+保存則 2 検算(残差・物)=0/L4 実データ 50,000 呼=10.00 呼/体/日 ≤上限・L6 偽艦隊 2 本で peak in-flight 25 ≤64。tests/c6 85 passed。
- **書式・役割語率(tools/c6/format_role_rates.py・smoke-2 テープ n=833)**: 書式エラー率 実効 0.042/厳密 0.101・別名 0.062・辞書写像 0.008・未定義 0.0024・**役割語率 0.0**(24 tick=0:00-0:24 では開閉店/補充の場面が無い→1 日ラン c6-day-1 で再測中)。
- **指標 B の事前登録再測**: 場面収集(mock 1 日・5,000 体)=実行中。在庫(サブ P): 営業時間外 1,375・到達不能 126・所持金不足 0・満員 0・断られた 0(5 種のうち 3 種が 0)→制約種の差し替え=D-30。再呼は 8B vs 14B INT8(W17 完了後に 14B を起動)。**D-30(a) で再収集(09-09 15:40・mock 5,000 体×1 日=50,000 場面)**: closed 1,375・unreachable 126・bad_target 6,862・no_stop 3,708・no_train 3,604 → 各 50 場面+決まる場面 100=在庫 OK(`data/bench/c6_alt/scenes.jsonl`・251 MB・gitignore)。
- **ablation①(固定枠 vs 単一ランキング)**: サブ R が切替口を実装(`--budget-mode {fixed,ranking}`・B2+B4+B4b を 1 本の池 250 tok・B5 池 220 tok・顕著性順位=`rank_by_saliency` 再利用・既定 fixed はバイト不変)。**親実行(09-09 17:10・実艦隊・5,000 体×24 tick・seed 1)**: 切替 24/24 場面でバイト差・②2 腕: fixed 833 呼 書式エラー率 0.058/ranking 0.036・未定義 0/0・呼あたり tok 900.8/903.1・**行動分布 JSD 0.0397 bit**・final_hash 不一致(プロンプトが違うので当然)。→ 固定枠の「意図的歪み(B5>B2)」の効果は行動分布 JSD 0.04 として計測可能=指標 B の帰無参照(seed 違い 0.0035)の 10 倍。合否線は設計に無い(報告値)。
- **会話相手の ID 順バイアス(サブ P・T5 長地平)**: `engine.commit.pair_partners`(セル内最小 id)で 240 tick・予算無制限のとき会話ターン起床のみ r=−0.136(個体 +0.006・セル −0.027)=v1 C-8 と同種→D-31。

## 3b. 親検収(C6 コード全体・09-09 17:20)
- 全体 pytest **1,772 passed / 0 failed / 1 xfail**(tests/llm 202・tests/c6 85・tests/perception 271 系・tests/engine 会話招待 15+結線 20)・lint 5 kept・scan CLEAN。
- mock 経路(実資産・5,000 体×1 日)×2: checkpoint **eb7e07ccab05ec72…** 一致(T2)・呼 50,000・parse_error 0・**conversation_sessions 67**(C4 期の 401 から減少=会話招待の設計準拠化で「相手が自分の呼で承諾を決める」形になり、MockLLM は 1/12 でしか 会話 と答えないため。実 LLM では相手の判断が正=D-36)。内訳: llm 9.6 / arbiter 2.3 / movement 0.14 ms/tick。

## 4. 層2 独立検収(別インスタンス Fable・09-09 17:45)
**判定=条件付き合格(重大 0/中 5/軽 8)**。層2 が自分で実行: pytest 1,796 passed/1 skipped(SHIBUYA_C6_FLEET_COUNTERS 未設定)/1 xfail・lint 5 kept・scan CLEAN・設計書の削除行 0・golden(テンプレ SHA 161fe181・fixed の prompt_hash bb23f7c6…)が HEAD と一致・mock 実資産 ×2 同一(eb7e07cc…)・`--budget-mode ranking` は mock では同一ハッシュ(MockLLM が本文を読まない=登録簿に明記どおり)・HEAD 版 src で fbad9b81/401 を再現=変化は会話招待由来。
- 中(コミット前に修正→サブ O): ①被招待の承諾返事が Phase C の `_apply_talk` で PARTNER_BUSY 失敗として個体状態に記録される(200 体 1 セル 700 tick で 45/51 セッション) ②承諾判定が返事の対象を無視(第三者名指しでも成立) ③退化検査ハッシュの記録更新(fbad9b81→eb7e07cc・本報告 §1 で更新) ④PENDING D-28/D-29 行の分断(親が修復) ⑤招待の不応期 60/30 分は契約書 §6(知人出現/傍受)からの転用=expedient 宣言が要る。
- 軽(記録): `[艦隊]` 行の繰り延べに error_other を含まない・終端 drain の応答は適用されない(計数のみ)・peak in-flight は予約数・T5 テストの docstring が旧規則・相互指名経路の不応期未刻印・テスト名の過大・drain 分の書式エラーが診断列に入らない・STATUS/IMPLEMENTED/devlog 未更新(親のコミット時)。
- 親の対応: ①②⑤+軽 4 件=サブ O(修正後に再検証)・③④=親(本報告・PENDING・登録簿)。
- **サブ O の対応(09-09 17:50)**: ①`_settle_pending_invites` を Phase C の前に新設(承諾行を intent から外す=偽 PARTNER_BUSY なし・テスト固定) ②承諾=会話かつ対象が招待者(または対象なし)・第三者名指しは拒否+新規招待 ⑤不応期 60/30 の転用宣言・相互指名にも検査/刻印・軽 4 件。pytest 1,779 passed・lint 5 kept・scan CLEAN。mock 実資産: checkpoint **8aaf3c74…**・呼 49,932・sessions 57(invites 1,114/accepted 57/declined 582/expired 471)。**新所見: 被招待者がプロンプト上で招待を知らないため承諾が出ない**(B6 に被招待の行が無い=知覚側の穴)→サブ R へ(テンプレ SHA を保てる形か・不可なら v1.1 の delta 案=D-21)。

## 4b. 指標 B 事前登録再測(親実行・09-09 17:45-18:25・tools/c6/metric_b_rerun.py・docs/bench/c6/metric_b_rerun.md)
- 場面: mock 1 日 5,000 体の 50,000 呼から D-30(a) の 5 制約種×50+決まる場面 100=350 場面(在庫: bad_target 6,862・no_stop 3,708・no_train 3,604・closed 1,375・unreachable 126)。2 モデル同プロンプト再呼(温度 0・seed 固定): **Qwen3-8B INT8 vs Qwen3-14B INT8**(検証 T2)。
```
モデル            n    違反率   書式エラー率  未定義率
Qwen3-8B-int8    350   0.1229   0.0000      0.0000
Qwen3-14B-int8   350   0.0029   0.0229      0.0000
Δ違反率(B−A) −0.1200  95%CI [−0.1543, −0.0886]  |上限| ≤0.05 → 不合格
行動分布 JSD 0.2909 bits  帰無 95th 0.0156(同モデル seed 違い=温度 0 で 0.0000) → 不合格
```
- 所見: **8B は制約場面(営業時間外・停車しない・列車なし・対象特定不能・到達不能)で 12.3% が制約違反、14B は 0.3%**。決まる場面を含む行動分布も 2 モデルで大きく違う(JSD 0.29)。品質プローブ v0(合成場面)の「決まる場面の分布は 3 モデル不変」は**実データ場面では成立せず**=v1 予算(T1=8B)の品質側裏付けが崩れる→**R16 の本決定の材料(D-37)**。帰無参照が温度 0 で退化(0.0000)している点は測定装置の改善点(帰無=温度 0.7 seed 違い、または 8B の 2 量子化版比較)。

### 4c. 層2 再確認(修正分・09-09 19:40)
**判定=条件付き合格(重大 0/中 2/軽 6)**: 前回の中-1/2/4/5・軽-2/4/5/6 は閉じたことをコードとテストで確認(承諾側の last_result/fail_streak 不変・第三者名指し不成立・不応期の転用宣言・相互指名の刻印)。pytest 1,832 passed・lint 5 kept・scan CLEAN(47 ファイル)・テンプレ SHA 不変・W17 再現(3 出力の SHA 一致・modified 0.187/JSD 0.421)・mock ×2 一致(d5d79337…)。T5 の論拠(kind 交絡)を自前 OLS で再現(生 −0.123→部分 −0.009)。
- 条件: ①本報告 §1 のハッシュ履歴(上で更新) ②T5 の判定格付け(反転 KS/JSD は傍証・層内 ρ+部分相関を主判定・符号反転の釘付け)=サブ P。軽: 相互指名の二重計数の疑い・名指し経路の e2e 承諾テスト不足・登録簿の ID 塊の文言・tests 本数の記録差・diff の見方。
- 次工程への懸念: 招待 TTL 3 tick と被招待起床の優先度が会話成立率を決める(D-36)/固定枠の個体群予算が最悪 262/300・単一ランキング 294/300/適応 raking は目標 0.19 に貼り付く(修復率の監視行を常設)。

## 5. 追記予定
- 指標 B 再呼(8B vs 14B)・役割語率(1 日ラン再測)・C6 出口(コミット・IMPLEMENTED)。
