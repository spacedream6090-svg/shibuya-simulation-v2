# /goal 条件文(C5〜C8 通し・無人運転用)

> 使い方: 下のコードブロックの中身を丸ごとコピーし、Claude Code の入力欄に貼って送信する(先頭の `/goal ` を含む)。自動モードで実行。長さ約 2,700 字(上限 4,000 字)。短縮投入形は末尾。
> 前提: 実装計画書 §9.1(工程表)・§9.5(判断規則・コミット規律=層2合格後に親がブランチへコミット・main マージはユーザー)・§9.6(層2検収・停止句)。ブランチ=`build/c5-c8`(main d413d61 から作成・09-09)。
> 外部前提: (1) 町丁目境界データ=受領済み(09-08) (2) GPU 艦隊(R16 仮決定=自前 vLLM・7×A5000)=**サーバー返却延長(U-2)が未定**。艦隊が使えない間は W14/W15/W17・C6・C7 に入らず停止句 (c) で止まる。

```text
/goal 実装計画書 docs/design/v2-implementation-plan.md §9.1 の工程 C5→C6→C7→C8 を順に完成させる。各工程の完了は§9.1の「検収(親・機械)」列を満たし、その証拠(コマンドと出力)をこの会話に表示したうえで、§9.6 層2の独立検収(Agentツール model:"fable" のレビュー役に docs/ops/layer2-review-checklist.md を渡し、pytest 再実行・設計適合・expedient登録簿更新・台帳不変・data/未追加・holdout 未開封 を確認した合格報告を得る)に合格し、親がブランチ build/c5-c8 へコミットしたことをもって成立とする。
達成条件(すべて): (1) C5-a: W16 母集団合成が build パイプライン(RUN_ORDER・build_manifest)に登録され、実データで w16_population.parquet と w16_gates.json が生成され、ゲート(SRMSE 性別<0.01・年齢<0.13・方面 JSD<0.01・空セル 0%・80 町丁目の人口一致)が全合格、再構築で W16 ヘッダ hash が一致、engine が W16 から個体を初期化して 5,000 体×1 日の mock ランが保存則を保って通る。店舗参入資本が経済センサス按分に置換され貨幣供給比が報告されている。 (2) C5-b: W14 看板文(2,337 呼・T2・温度0・同店 2 回生成でバイト一致・固有名詞は入力由来のみ)と W15 セル静的文(≈600 呼・同セル 2 体で差分ゼロ)が凍結 SHA つきで生成され、W17 スケジュール(T1・40 万呼・週 7 日)が PT 時間帯カーブとの JSD と修正率<20% を満たし、W18 被覆指標が再算出されている。 (3) C6: 艦隊接続(httpx×7・cache_salt・sha256_cbor)・実 LLM 24step スモーク(5,000 体)・T3〜T9(バッチ不変性・リプレイ・封印・dirty 禁止)・指標 B の事前登録再測・ablation①(固定枠 vs 単一ランキング)・書式エラー率≤0.10・役割語率の再測・L4/L6 予算内。 (4) C7: 40 万体×1 シミュ日(週 7 日表・第1陣全部)が壁時計≤24h・M8≤24GB・S1≤5GB・診断行が出て、同 seed 再ラン一致(T2)、被覆指標 WC-6、holdout 照合は事後 1 回のみ(KDDI 形状 5 指標)。 (5) C8: ablation 第1陣 6 本+広告ゼロ・expedient 登録簿の感度試験(「結果を駆動していない」証明)・忠実度計器盤。 (6) 各工程出口で docs/ops/build-report-Cn.md を書き、devlog に工程出口エントリ、PENDING→IMPLEMENTED の移動、予算書の実測欄・expedient 登録簿の追記を行っている。
証明の規則: 証拠は親(この会話の Claude)自身が実行した pytest / lint-imports / scan_secrets / ビルド・ランのコマンド出力のみ。サブエージェントの完了報告は証拠にしない。各工程の出口で層2レビュー役の合格報告を会話に貼る。
制約: docs/design/ 配下の設計書の決定項を書き換えない(expedient 登録簿への追記のみ可)。data/ を git に追加しない。main へコミット・マージしない(ブランチ build/c5-c8 のみ)。同時起動するサブは 2 本まで・サブは子サブを起動しない・サブはコミットしない。holdout(w19_freeze.json の封印対象)は C7 の事後照合 1 回以外で開かない。実 LLM は R16 仮決定(T1=Qwen3-8B INT8 DP7/静的文=Qwen3-32B AWQ TP4 温度0/検証 T2=Qwen3-14B INT8 TP1)に従う。CLAUDE.md §5・§7 を守る。
停止句(この停止もゴール達成として扱う): (a) パターン台帳の行・予算宣言(P/M/S/L)・ライセンス台帳・憲法6原則・設計書の決定項に触れる分岐に出会った場合は、その分岐を PENDING.md に書いて停止する。 (b) 世界カタログ分母 SHA を再現できない場合は PENDING.md に書いて停止する。 (c) GPU 艦隊(SSH エイリアスのサーバー)に接続できない・返却延長が未定で W14/W15/W17・C6・C7 に入れない場合は、その時点までの工程をコミットし PENDING.md に状態を書いて停止する。 (d) 外部データの新規取得が必要で CLAUDE.md §7 の公的ドメイン例外に該当しない場合は停止する。 (e) 300 ターン経過で停止する。
```

## 短縮投入形(リモートコントロール等で長文を貼れないとき)

```text
/goal docs/ops/goal_c5_c8.md のコードブロック内の条件文(Claudeが最初のターンで全文を会話に表示する)をすべて満たす。停止句に該当して停止した場合も達成とみなす。or stop after 300 turns
```

## 開始前チェック(ユーザー側)
1. PC のスリープ無効・VS Code(セッション)を開いたまま・自動モード(09-08 確認済み)。
2. 使用量クレジット=使わない(09-08 決定)。5 時間枠に当たれば待って自動再開。週次制限は手動 Enter。
3. **U-2 サーバー返却延長の可否**を決めて伝える(C5-b 以降の前提)。延長不可なら返却前チェックリスト(HF キャッシュ削除・~/bench 退避)を親が実施してから C5-a までで停止する。

## 親側の準備(済)
- ブランチ `build/c5-c8` を main(d413d61・PR #1 マージ後)から作成(09-09)。
- サブ J(W16 母集団合成)・サブ K(店舗参入資本按分)を起動(09-09・/goal 投入前でも作業は進む)。
