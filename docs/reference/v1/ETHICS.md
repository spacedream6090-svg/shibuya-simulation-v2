# 倫理方針(ETHICS)

本リポジトリは、渋谷を舞台にした**大規模 LLM 人工社会シミュレーション**の研究・開発コードである。
本方針は AUTOMATA 第2回ハッカソンの公開要件、および設計文書 [`docs/design.md`](docs/design.md)(D17)・
リスク台帳 [`docs/risk-register.md`](docs/risk-register.md)(R17)に基づく。

---

## 1. 研究概要

- **問い**: 「世界を変えようとする個体」は生まれつき存在するのか、環境から創発するのか。
  改変者を直接実装せず、経験と社会環境で変化する内部状態から自然発生するかを**観測**する。
- **測定形**: 世界改変量(空間・資源・象徴・network の4層への連続的な書き換え量)を、
  初期パラメータで回帰した決定係数 R²(k) の変化と、seed 発散・早期警戒シグナルの三角測量で捉え、
  相転移点 k* を探す。`k` は「ソロ内省での信念の書き戻し自由度」として操作化する。
- 位置づけは**方法論**(創発と世界改変が死なない人工社会の作り方 + その測定の厳密さ)であり、
  特定の政治的・社会的主張を行うものではない。対外的には collective action / institutional change の
  枠組みで記述する。

## 2. 倫理原則

1. **実在の個人・団体は一切登場させない。**
   場所・施設名は実在の渋谷のもの(駅・通り・商業施設など)をリアリティのために使用するが、
   そこに登場する人物・企業・団体・その発言や行動は**すべて架空**である。
2. **シミュレーション内の全出来事・発言・人物・組織はフィクションである。**
   現実の出来事・実在の人物の見解を表すものではなく、現実の予測・評価・断定として用いてはならない。
3. **ペルソナは手続き的に生成された合成人格である。**
   国勢調査(渋谷区)の統計的な周辺分布を IPF で骨格化し、心理尺度分布を独立サンプリングして
   文章化したもので、**特定の実在人物をモデル化していない**([`scripts/build_personas.py`](scripts/build_personas.py))。
   氏名はランダムな一般的な姓名の組み合わせであり、実在人物との一致は意図しない。
4. **abliterated / 無検閲モデルの利用は「統制目的」に限る。**
   RLHF 由来の同調・均質化バイアスが測定を交絡させるため、その統制対照として用いる。
   これらのモデルの**生出力は公開しない**。公開するのは集計・指標・可視化などの派生物に限る。
5. **判定(LLM-judge)は補助指標に留める。**
   「世界を変える人か」の意味判定は循環の恐れがあるため、主指標は常に客観カウントとし、
   judge は人手または判定間 κ ≥ 0.7 のときだけ補助的に採用する
   ([`scripts/judge.py`](scripts/judge.py)、R4)。judge はシミュ本体へ逆流しない。
6. **発話は注入経路になりうるため sanitize・上限・構造化を課す**(R15)。異常な伝播は観測層で監視する。

## 3. データの扱い

- **合成データのみ。** 本リポジトリが生成・格納するログ(`runs/`)は、架空エージェントの
  行動記録であり、実在の個人データ・個人情報を一切含まない。
- **外部統計は集計値の周辺分布のみを利用する。** 渋谷区の年齢・性別・職業・昼夜間人口などの
  **公表された集計統計**をペルソナ骨格の分布として使う([`data/shibuya_population.json`](data/shibuya_population.json))。
  個票(ミクロデータ)は使用しない。数値の出典・取得状況・未確認箇所は
  [`docs/research/lexicon-ipf-shibuya.md`](docs/research/lexicon-ipf-shibuya.md) に明記し、
  **推定値を実数と偽装しない**(取得できないものは「暫定/未確認」と正直に記録する)。
- **検索・SNS・ニュースはシミュ内の閉じたデータベース**で実装し、実 API・実サービスへは接続しない
  (再現性の担保と、架空世界の閉性のため)。
- **地図タイル**は可視化時にオンラインで OpenStreetMap を参照することがある
  (© OpenStreetMap contributors)。オフライン時は無地で描画する。

---

疑問や懸念は Issue で提起してほしい。上記に反する利用(実在人物の模倣、生出力の無断公開、
現実の予測としての流用など)は本プロジェクトの意図に反する。
