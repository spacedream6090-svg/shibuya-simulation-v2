# C9b 実 LLM スモーク(c9b-smoke-s1)— 親報告(2026-09-17・第222・D-84 (b))

- **ラン**: 5,000 体 × 1,440 tick・seed 1・`--geometry edge --vocab-version v2`・艦隊 7×Qwen3-8B INT8・`--mode production`・HEAD 49fca80(FleetBridge の vocab_version 修正入り)。17:36〜18:20・**壁時計 2,626 s(43.8 min)**・exit 0。
- **保存則 OK**(139,575,428 不変・最小在庫 0)・日次センサス **PASS**(残差 0)・checkpoint `16e6a44b…`・edge: ホップ反復 12,188・**詰まり 0**。
- **LLM 呼 35,856 = 7.17 呼/体/日**(c7-day-4 の 6.94〜7.00 より +3%)。書式エラー率 実効 0.006 / **厳密 0.162(受入 ≤0.10 を超過)**・位置読み 0.105(3,748 呼)・別名 0.068・温度 0 再生成 16,783(成功 1,299)・未定義 20。

## 1. G3〜G6(対象と注意)の成立率=**ほぼ 0**

| 量 | 実測 | 台本 LLM(第214・2,000 体) |
|---|---|---|
| 近づく(到達 / 対象不在) | **0(0 / 0)**(応答に「近づく」を含む呼 36・「近寄る」3) | 2,795(到達 569) |
| 注意の焦点 取得 / 消失 | **0 / 0** | 2,386 |
| 会話 実距離成立 | **9**(「会話」を選んだ呼 18) | — |
| 見る | 4 呼 | — |

**含意**: 語彙 v2 の白リスト(24 語+食事)を見せる `intent_mode=vocab` では、8B は「近づく」「見る」を**ほぼ書かない**(辞書 v4 の同義語は用意したが出番が無い)。G3〜G6 を測るには **hint 腕(AB7b の型)か open 腕**で回す必要がある=切替口は既にある(`--intent-mode hint`)。会話 18 呼(0.05%)は c7-day-4 の 0.15% より低い。

## 2. 行動分布(応答の「行動:」欄・35,856 呼)

食事 **15,479(43.2%)**・移動 13,351(37.2%)・休憩 3,497・購入 1,772・待機 895・乗車 563・探索 130・通報 52・通勤 46・会話 18・観察 13・進入 11・断る 7・退去 7。
**食事を選んだ体 4,711 / 5,000・1 体あたり中央値 3 回/日・最大 14**。理由欄の最頻は「食事の必要性が高い」(11,982 呼)。→ v1 では「食べる→購入」に写っていた分が食事に移ったのか、食事が過剰に選ばれているのかは **AB7c(v1 vs v2・実行中)** で判定する。1 日 3 回の中央値は食事回数として不自然ではないが、購入(1,772)が v1 相当より減っている可能性がある。

## 3. 新しい欠陥候補: **対象欄にテンプレートの説明語が写る**

対象≠なし は 21,744 呼(60.6%)だが、その上位が **「物のカテゴリ」9,055・「食品のカテゴリ」568・「セルID」398**=観測ブロックの**欄の説明文をそのまま値として書いている**(few-shot の写し事件と同型・第164 の 47%)。実在名(food0183・カフェ・ベローチェ・大連餃子基地・渋谷駅)は残り。位置読みエラー 0.105 と厳密書式 0.162 の一部はこれが原因の可能性(未確定)。
- **対処案**(判断待ち): (a) parser 側で既知の説明語(物のカテゴリ/食品のカテゴリ/セルID)を「なし」に正規化し件数を診断欄へ(=欠陥修正・切替口・文面不変)(b) テンプレートの欄説明を例示から外す(=文面凍結に触る・版と感度試験が要る)。**親推奨 (a) を先に・(b) は次の文面版で**。

## 4. 結論

- D-84 (b) の問い「G3〜G6 は実 LLM でどれだけ成立するか」→ **vocab 腕では成立しない**(近づく 0・焦点 0)。C9b の機構は台本 LLM で end-to-end 実証済みなので、**実 LLM 側の語の出し方(hint/open)が律速**。
- 40 万体で edge+v2 を回す前に: ① AB7c の結果(食事 43% の是非)② 説明語写しの正規化 ③ 厳密書式 0.162 の内訳(位置読み)を見る。
- テープ・occupancy・checkpoints は `data/server_retrieval/2026-09-17/{tape,runs}/c9b-smoke-s1/`(gitignore)。
