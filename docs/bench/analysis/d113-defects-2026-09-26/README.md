# D-113 実装の欠陥 4 点の修正記録(2026-09-26〜・1 欠陥 1 commit・版ごとの checkpoint)

> 位置づけ: [壁打ちの決定記録 §2 D-113](../../../design/v2-wallbounce-decisions-2026-09-25.md)(ユーザー決定 09-25: 4 点とも直す・**1 欠陥ごとに 1 commit**・そのたびに **mock 5,000 体の既定 checkpoint を記録**・**旧 checkpoint は消さず版を分けて残す**・実 LLM のベンチは回さない)と 09-26 の答え 5(④ は未測定と宣言し GPU を借りたら測る項目へ)。欠陥の出所は [顕著な出来事の台帳](../notable-events-2026-09-24/README.md) §2/§5。
>
> 計測の作法(親の補正・記録 注記 4-5): モックの方針は LLM の言い換えを出さないので、**パーサの欠陥(①)はモックでは測れない**。既存テープ(C8・8 本・485,527 呼)の応答を修正前後のパーサで全行読み直し、呼ごとに突き合わせる(**リプレイ計測**・GPU 不要)。②③ はエンジン側なのでモックの checkpoint と集計で測れる。④ はプロンプトが変わるのでどちらでも測れない。

## 0. 版ごとの checkpoint(mock 5,000 体・seed 1・`python -m shibuya.cli --agents 5000 --seed 1 --world data/world/v2 --checkpoints-out …`・既定=読み口 v2)

| 版 | commit | 変更 | tick 359 / 719 / 1079 / 1439(combined 先頭 8 桁) | final |
|---|---|---|---|---|
| v2 既定(修正前) | 630ad13(main・PR #4 マージ後) | — | 7382aeea / d5790f81 / aad9c915 / ba01bd0b | `ba01bd0beed19666…` |
| D-113 ① | 第266 | パーサ: 未知ラベルを値として読まない+テープの別名 7 語 | **同一**(7382aeea / d5790f81 / aad9c915 / ba01bd0b) | 不変(想定どおり=モックは言い換えを出さない) |
| D-113 ② | 第267 | 通報の前提「当該事象を知覚済み」の検査(`--report-precondition on`・既定) | **d2752886 / 4ba1528d / 3682ea4e / 2f3969cf** | `2f3969cfe29b2b76…`(通報 2,789 呼が全て失敗=このランは顕著行為 0 件) |
| D-113 ② 切替 off | 第267 | `--report-precondition off`(帰無腕) | 7382aeea / d5790f81 / aad9c915 / ba01bd0b | **修正前と同一**(切替口の検証)。D-62 の退化検査(`tests/engine/test_presence_executor.py` の null arm golden)はこの切替で旧 golden を再現する(第267b) |
| D-113 ③ | 第268 | 満席の列を席が空いた分だけ並んだ順に捌く(`--queue-service on`・既定) | **同一**(d2752886 / 4ba1528d / 3682ea4e / 2f3969cf) | 不変=既定の mock 5,000 では列が立たない(待ち行列 0)。列が立つ感度腕(1 人あたり 30 m²)では on a62c7f17 / off d81af637 |
| D-113 ④ | 第269 | B0 の末尾に役割語 12 語の 1 行(`--role-words on`・既定) | **同一**(d2752886 / 4ba1528d / 3682ea4e / 2f3969cf) | 不変=mock は B0 を読まない。B0 の指紋 `b0_sha256`: off 2b4bfc8a(凍結値のまま)/ on **24661cd6**。効果は**未測定**(GPU を借りたら測る項目) |

## 1. ① パーサが別名表に無いラベルを対象の値として読む(第266)

**欠陥**: `parse_two_line` は「理由/行動/対象/ひと言」のラベルを別名表([parser.py](../../../../src/shibuya/llm/parser.py) `LABEL_ALIASES`)で走査する。表に無いラベル(「目標:」)は走査に掛からず、行動欄の値が「移動 目標: 飲食店」になる。位置引数の回収 `_fill_positional` が余りの先頭トークン「目標:」を**対象の値**として採り、`format_ok` は True のまま(=書式エラー率に出ない)。値「飲食店」は捨てられていた。

**修正**(切替口は無し=パーサは決定論の純関数。受入指標 `strict_format_ok` は V0 表のままで分母を動かさない):
1. テープで実測された表層 7 語を別名表 `LABEL_ALIASES_D113` に足す(→ 対象): 目標 32,736 呼・カテゴリ 1,984・目的先 523・ターゲット 378・对象 127・目标 57・行く先 19(8 テープ合計・修正前の対象欄に「語+コロン」だけが入っていた 36,123 呼の内訳上位)。C6 の別名と同じ扱い(`alias_used`・`strict_format_ok` は False)。
2. 一般則: 位置引数の回収で「語+コロン」だけのトークン(`^[^\s:：]{1,10}[:：]$`)は**値ではなく未知のラベル**として読み飛ばし、診断 `unknown_label:<表層>` を `errors` に残す。後ろに値が無ければ欄落ち(`format_ok` False)。
3. `positional_used` は `positional:` の診断があるときだけ True(未知ラベルの読み飛ばしだけでは立てない)。
- テスト: [tests/llm/test_parser.py](../../../../tests/llm/test_parser.py) に D-113 の 6 本(+別名表の形の検査を更新)。parser 系 134 passed。

**リプレイ計測**(8 テープ・修正前後を呼ごとに突き合わせ・[d113_1_parser_replay_diff.json](d113_1_parser_replay_diff.json)・手順 [d113_1_parse_snapshot.py](d113_1_parse_snapshot.py) → [d113_1_parse_diff.py](d113_1_parse_diff.py)):

| テープ | 呼数 | 対象の読みが変わった呼 | 対象が「語+コロン」だけ(前 → 後) | format_ok T→F / F→T | 後: 未知ラベルを読み飛ばした呼 | 行動欄に未知ラベルが残る呼(前 → 後) |
|---|---|---|---|---|---|---|
| AB6c ad_zero | 74,581 | 4,019(5.4%) | 3,586 → 0 | 32 / 3 | 67 | 41 → 2 |
| AB6c p_see_0.14 | 73,635 | 4,488(6.1%) | 4,019 → 0 | 21 / 2 | 64 | 36 → 1 |
| AB6c p_see_0.30 | 73,753 | 4,974(6.7%) | 4,409 → 0 | 32 / 2 | 58 | 43 → 1 |
| AB6c p_see_1.00(=AB8 無制限) | 72,800 | 7,593(10.4%) | 6,769 → 0 | 30 / 3 | 48 | 81 → 4 |
| AB8 ×0.5 | 19,856 | 1,985(10.0%) | 1,779 → 0 | 6 / 0 | 20 | 17 → 1 |
| AB8 ×1 | 36,906 | 3,641(9.9%) | 3,284 → 0 | 12 / 0 | 31 | 28 → 2 |
| AB8 ×2 | 61,196 | 6,170(10.1%) | 5,508 → 0 | 26 / 2 | 59 | 56 → 1 |
| **合計** | **485,527** | **40,463(8.3%)** | **36,123 → 0** | **189 / 15** | | **383 → 16** |

読み:
- 欠陥は呼の **8〜10%** の対象欄を壊していたが、`format_ok` の変化は合計 204 呼(0.04%)=**書式エラー率には見えない**欠陥だった。
- 修正後の対象の上位(×1): 飲食店 516・食品 494・物のカテゴリ 450・「食品 カテゴリ」314・食品カテゴリ 185・食堂 120。「物のカテゴリ」「食品カテゴリ」はテンプレートの欄説明語の写し(D-89 で既知)で、対象なしとして扱われる。
- `format_ok` T→F の 189 呼は、未知ラベルの後ろに値が無かった形(「行動: 移動 目安:」)が欄落ちとして正しく落ちたもの。
- 残る 16 呼は 10 文字超のトークンや行末以外に埋まった形で、一般則の対象外(放置)。
- **世界への効き**: 対象欄は会話の相手と(C9b の腕でのみ)移動の行き先に使われる(第262 ④)。既定の経路では購入先・移動先はエンジンが選ぶので、この修正が本番の結果を変えるのは D-112 ②(行き先を対象欄から)を入れた後。checkpoint が不変なのはそのため(モックの応答に未知ラベルが無いことも理由)。

## 2. ② 通報の前提「当該事象を知覚済み」が未検査(第267)

**欠陥**(第263 ②): 行動契約書 §2.1 の通報の前提は「通報先が存在・当該事象を知覚済み」だが、`resolve._apply_record_only` は検査せず必ず成功にしていた。C7 の 39 万体テープでは通報 423 呼のうち観測に事象があったのは 7 呼。D-57(過剰通報)の (b) と同じ処方。

**修正**(切替口 `--report-precondition {on,off}`・既定 on・`run_day(report_precondition=)`・`resolve.apply(salient=, report_precondition=)`):
1. `SalientProcess.event_seen_tick`(体ごとに「顕著行為の行が自分のセルの B4 に載った最後の tick」・−1=未)。B4 の事象の行は**セル単位**(`_build_cell_lines`)なので、事象のセルに居た全員=観測に行が出た全員を知覚済みとする(`p_notice` は起床の到達判定で別)。予算で落ちた事象・報道(broadcast)は載らないので数えない。
2. `resolve._apply_report`: `tick − event_seen_tick ≤ REPORT_WINDOW_TICKS`(5 tick)なら成立、それ以外は `BAD_TARGET`(通報すべき事象が無い)。窓 5 tick は t_apply = 起床 + δ_perc + δ_think の最長レーン L3(300 s)からの導出値(mock/L1 は 1 tick)。効果先(事象レコードに通報 1 件 → 検知確率)は未実装のまま=成立しても状態は変えない。過程 OFF(`salient=None`)なら従来どおり(C2 互換)。
3. 診断: `ResolveOutcome.n_report_ok / n_report_no_event` → `RunResult` に集計し summary に 1 行(通報 0 のランでは summary 不変)。
- テスト: `tests/engine/test_resolve.py` +4(前提・窓の境界・切替 off・断るは不変)・`tests/engine/processes/test_salient.py` +2(事象のセルの全員に tick が付く・窓は閉区間)。resolve+salient 47 passed。

**計測**(mock 5,000 体・seed 1・15 s/ラン):

| ラン | 顕著行為 | 通報 成立 | 通報 失敗(知覚済みの事象なし) | checkpoint |
|---|---|---|---|---|
| 修正前(第266) | 0 | 2,789(全て成功=検査なし) | 0 | ba01bd0b |
| 修正後・既定 on | 0 | **0** | **2,789** | 2f3969cf |
| 修正後・off(帰無腕) | 0 | 2,789 | 0 | ba01bd0b(修正前と同一) |
| 修正後・on・発生率 2,000/万体/日(結線の確認) | 2,020(気づき 103,287) | **801(24.7%)** | 2,444 | —(感度の確認だけ) |

読み: モックは行動を一様に引くので通報は呼の 8.4%(2,789/33,090)で、既定ランは顕著行為 0 件(期待値 1.5/日・P(0)=22%)のため全て失敗する。発生率を上げた確認ランでは事象のセルに居た体の通報が成立する(24.7%)。実 LLM では通報の 98% が事象なしで選ばれていた(第263)ので、同じ検査で通報の実現量はほぼ 0 になる見込み=**「過剰通報」は結果コードに出る**ようになる(D-57)。checkpoint の変化は `last_result`/`fail_streak` の差から。

## 3. ③ 満席で並んだ体を捌くコードが無い(第268)

**欠陥**(第263 ⑤・D-94): `_apply_buy`/`_apply_eat` は満席のとき体を待ち行列へ入れて `OK` を返す(契約書 §2.1 のコスト欄「価格・待ち時間」)が、席が空いても誰も列を捌かず、並んだ体は 15 tick 後に必ず `INTERRUPTED`(`crowd.step` の離脱閾値)になっていた。C8 s1 では食事の失敗の 6.5%(約 1,000 件)がこの「並んで打ち切り」。

**修正**(切替口 `--queue-service {on,off}`・既定 on・`run_day(queue_service=)`・`resolve.apply(queue_service=)`):
1. `CrowdProcess.queue_action`(並んだ目的: 0=購入・1=食事・−1=並んでいない。registry に欄を足すと M 行と checkpoint の意味が変わるので過程側に置く)。並ぶときに `_remember_queue_action` で控える。
2. `resolve._serve_poi_queue`: Phase C の**新しい意図の適用より前**に、並んでいる体を `(queue_since, agent_id)` 昇順=並んだ順に並べ、`CrowdProcess.can_admit`(行順で席を詰める)で空いた席の分だけ入れ、並んだときの行動を `_complete_buy`/`_complete_eat`(`_apply_buy`/`_apply_eat` の後半を関数に切り出した=挙動不変)で完了させる(支払い・棚・空腹・在席)。待つ間に閉店した店の列は解散(`CLOSED`)、払えなくなった体は `MONEY_SHORT`、棚が空なら `OUT_OF_STOCK` で列を離れる(席を取る前に離れるので空いた席は同じ tick に次の体へ)。15 tick の離脱閾値はそのまま。
3. 診断: `ResolveOutcome.n_served_from_queue / n_queue_closed` → `RunResult` に集計し summary に 1 行(列が立たないランでは summary 不変)。
- テスト: `tests/engine/processes/test_crowd.py` +6(FIFO で席へ・新参より先に並んだ体が席を取る・切替 off で従来どおり 15 tick で `INTERRUPTED`・閉店で解散・払えない体は列を離れ次の体へ席が回る・混雑場なしでは何もしない)。crowd+resolve 51 passed。

**計測**(mock 5,000 体・seed 1):

| ラン | 待ち行列に入った | 席へ捌いた | 閉店で解散 | 離脱(15 tick・`INTERRUPTED`) | 購入(実現) | 売上 | checkpoint |
|---|---|---|---|---|---|---|---|
| 既定(1 人あたり 2.0/4.0 m²・on) | 0 | 0 | 0 | 0 | — | — | 2f3969cf(② と同一=不変) |
| 感度腕 30 m²/人・**on** | 751 | **343** | 4 | **375** | **1,274** | 932,000 円 | a62c7f17 |
| 感度腕 30 m²/人・off(第267 以前) | 541 | 0 | 0 | **524** | 1,150 | 866,400 円 | d81af637 |

読み: 既定の mock 5,000 では席が足りず並ぶ場面が無い(モックは一様抽選で客が分散する)ので checkpoint は不変。席を絞った感度腕では、並んだ体の 46%(343/751)が席へ入り、打ち切りは 524 → 375、実現購入は +124(+10.8%)。列に入る数が on で増える(541 → 751)のは、席が回転して同じ店に次の客が並べるようになったため。実 LLM では食事の意図が飲食店に集中する(第263 §5「上位 10 店の占有 ×2」)ので効きは大きくなる見込み。

## 4. ④ B0 の出力規約に役割語が並んでいない(第269)

**欠陥**(第263 ⑦・D-38): 行動契約書 §2.2 は「種別固有(役割語)は権限をエンジンが検査し、**語彙自体は全員に見せる**」と定めるが、B0 の出力規約は 12 語しか並べていなかった。パーサは役割語を受理するので、LLM が役割語を選べない(役割語 0)一因。

**修正**(切替口 `--role-words {on,off}`・既定 on・`run_day(role_words=)`・`Renderer(role_words=)`):
1. `templates.ROLE_WORDS_12`(契約の `ROLE_ACTION_WORDS` と同値・テストで守る)と `ROLE_WORDS_LINE`=「[出力規約] 役割語: 接客 / 補充 / 開閉店 / 価格改定 / 発車 / 停車 / 放送 / 遅延報告 / 計画改訂 / 指示 / 並ぶ / 撮影 も行動の語として書けます。自分の役割に権限があるときだけ成立し、権限が無ければ失敗します。」(全員に同文=規約⑧ を保つ)。
2. `b0_system(intent_mode, vocab_version, role_words=False)`: 有りなら本文の**末尾に 1 行**足す(open/hint/v2 のどの B0 にも同じ 1 行・`行動:` 断片は触らない)。既定の `b0_system()` は同一オブジェクト=`template_sha256`(161fe181…)と `b0_sha256("vocab")`(2b4bfc8a…)は不変。`b0_sha256(..., role_words=True)` は 24661cd6…。
3. レンダラの既定は False(描画バイトの凍結を保つ)・**ランの既定は True**(run manifest に `role_words` 列)。第268 以前に録ったテープの再生は `--role-words off`(B0 が変わると prompt_hash が変わる)。
4. 共有静的グループの予算: B0 603 + 役割語行 61 + B1 9 + B3 30 = 703 tok ≤ 750(§2.2)。
- テスト: `tests/perception/test_templates.py` +3(契約との一致・1 行追加と凍結・予算)・`tests/engine/test_intent_mode_open.py` +1(既定 on・manifest 往復・checkpoint は on/off で同じ)。perception+intent 全緑。

**計測**: **未測定と宣言**(ユーザー了承 09-26)。モックは B0 を読まないので checkpoint は不変(2f3969cf)、既存テープの再生はプロンプトが変わって当たらない。GPU を借りたら「役割語率」(第262 ④: 本番 0)を on/off の腕で測る(PENDING §0「GPU を借りたら測る項目」)。注意: 役割語はエンジン側に適用分岐が無く(`resolve._APPLY` は 12 語+継続)、選ばれても待機に落ちる(第262 ④)=効果先(C4 の役割行動)は別件。

## 5. まとめ(4 件・2026-09-26)

| 欠陥 | commit | 既定 checkpoint(mock 5,000) | 測れたもの |
|---|---|---|---|
| ① パーサの未知ラベル | 第266 | 不変 ba01bd0b | リプレイ 8 テープ: 対象の読みが 8.3% の呼で変わる |
| ② 通報の前提 | 第267 | ba01bd0b → 2f3969cf | 通報 2,789 呼が全て失敗(事象 0 のラン)・発生率↑で成立 24.7% |
| ③ 満席の列の捌き | 第268 | 不変 2f3969cf(列が立たない) | 感度腕 30 m²/人: 打ち切り 524 → 375・購入 +10.8% |
| ④ 役割語の提示 | 第269 | 不変 2f3969cf(B0 を読まない) | 未測定(GPU 後) |

切替口: `--report-precondition` / `--queue-service` / `--role-words`(いずれも既定 on・off で第265 以前の挙動)。① は純関数の修正で切替口なし(`strict_format_ok` が V0 の指標を保つ)。
- ③ 満席で並んだ体を捌く(並ぶ=成功扱い・15 tick で `INTERRUPTED`・席は食後 30 tick 占有=第263 ⑤)。モックの checkpoint と列の成立数で測る。
- ④ B0 の出力規約に役割語を並べる(契約書 §2「役割語も全員に見せる」と不一致=役割語 0 の一因)。**未測定と宣言**(モックは B0 を読まない・リプレイはプロンプトが変わると当たらない)。GPU を借りたら測る項目(PENDING §0)。
