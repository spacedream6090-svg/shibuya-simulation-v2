# D-68 下見(2,000 体・多腕)設計書 — 草案 v0(2026-09-11・未決定・決定項 §8)

> **状態: ユーザー承認(第153・2026-09-11)= §8 の決定項 12 件をすべて推奨で確定(1 プール employment・2 第31表按分・3 arrival_lead_min・4 生活の運びだけ・5 32B 200 体・6 週 7 日 1 呼・7 5 腕・8 改善率+フロア/弊害は絶対値・9 raking 12%/0% 両方・10 修正率を由来別に・11 JSD は実測後/判定は東京都・12 宿泊客の就寝地=宿泊施設)。実装開始(第153)**。設計サブ(Opus・読むだけ)の草案を親(Fable)が実読し、前提を変える 3 件をプールと W17 コードで**実測確認**した(第148): ①プールに `employment` 欄が実在(1,000,000 行・欠損なし: 正規 512,267/非就業 225,632/非正規 172,370/自営業主 50,873/役員 32,023/家族従業者 6,835)②L2 の `shift_pattern` は**全件同一ではない**(09:00-18:00 月金 67,266 が最多で約半分・09:00-19:00 月土 27,474・10:00-21:00 毎日 15,908・07:00-23:00 毎日 11,381・22:00-06:00 毎日 9,072・20:00-04:00 2,339 ほか。**端はすべて :00**。窓なしの体は種別既定で埋まる)=第146 の記述「L2 全件同一」は親の誤り ③W17 の出力文法 `LINES_PER_DAY_MIN/MAX = 4/5`(`prompt_regex` の `{4,5}`)が**活動数 5.8±0.46 を決めている**=どの腕も文法を緩めない限り活動数は動かない。`arrival_lead_min` は 155,449 行・111 値で実在。
> 前提: 答申 `docs/research/v2-d68-behavioral-diversity-research.md`(§6/§7/§11 親一次確認済)・ユーザー決定 第146(経路 1+2 を C7 前に下見で・LLM から自ずと・エンジン規則で担保しない)。

## §0 目的

- **問い**: 「本物の勤務窓(P1)」「自己生成の同一性(P2)」は、W17 の同質性を現実側アンカーへ動かすか。動かすなら本番 390,067 呼へ投入する。
- **出口**: 採否+採る場合の本番プロンプト版の確定。C7 再ラン・計画実行層とは**並行可**(層は `derive`/`native` 両対応で交絡しない)。
- **先に作るもの**: 読み取り専用の物差し `tools/w17/diversity_yardstick.py`(M1〜M8 を parquet と応答 jsonl から出す。`presence_yardstick.py` と同じ「前・後を同じ道具で」規約)。**腕を回す前に P0 の前値を凍結する**。

## §1 腕の定義(推奨 5 腕・呼数はほぼ同じ)

| 腕 | 事実(user) | 窓の出所 | system/例示 | 文法 | 修復 | raking | 呼 |
|---|---|---|---|---|---|---|---|
| **P0** | 現行 | プール `shift_pattern`/種別既定 | 現行(例示は全部 :00) | `{4,5}` | 現行 7 規則(duty_clamped 有) | 現行 | **0**(凍結資産を読む) |
| **P0b** | P0 と同一 | 同上 | 同上 | 同上 | 同上 | 同上 | 2,000(**同腕 2 標本のフロア**・seed だけ違う) |
| **P1a** | +雇用形態・自分の出勤/帰宅時刻(分単位) | **第31表から体ごとに逆関数法** | 例示の時刻に `:07`/`:23` を混ぜる | `{4,5}` 据置 | duty_clamp → **窓整合検査**(計数のみ) | 同上 | 2,000 |
| **P1b** | P1a と同一 | 同上 | 同上 | **`{3,10}`** | 同上 | 同上 | 2,000 |
| **P2** | P1b+**本人が書いた同一性 250〜350 字** | 同上 | ブロック形式(計画実行層設計書 §9)・面接由来 | `{3,10}`+`block_kind` | 被覆・深夜・就寝の検査 | 同上 | 4,000(2 段) |
| (比較)**P2-32B** | P2 と同一 | 同上 | 同上 | 同上 | 同上 | 同上 | 400(200 体×2 段) |

- P0b を足す理由=同じ腕の 2 標本の差(フロア)がないと、腕の差が効果か揺らぎか言えない。P1a/P1b を割る理由=窓と行数上限を同時に変えると M3(時刻)と M4(活動数)の帰属が付かない。
- 下見の出力は `w17_trial_<arm>_prompts.jsonl`/`..._responses.jsonl`(**本番 glob `w17_responses*.jsonl` に当たらない名前**)。parquet も `W17.header.json` も書かない=「報告だけ」規約。

## §2 P1 の勤務窓投入(データ → 体の写像)

1. **雇用形態**: プール `employment` → 第31表の行。正規→正規の職員・従業員(行動者率 89.1%・出勤平均 7:52)/非正規→正規以外(72.7%・9:03。**内訳 パート/アルバイト/契約/嘱託/派遣 は全国人数比で按分=expedient**)/役員→会社などの役員/自営業主→雇人のある・ない業主(28:72 で按分=expedient)/家族従業者→同/非就業→引かない。
2. **出勤時刻**: 第31表の 15 分ビン構成比の累積から逆関数法。**ビン内は 15 分一様に散らす**(分の丸めをここで断つ)。seed=`blake3("shibuya.build.sched/W17/v2/depart:"‖agent_id)`(既存規約と同形・ドメインだけ変える)。
3. **始業=出勤+片道通勤時間**。候補 (a) プールの `arrival_lead_min`(10〜120 分・体ごと・p10/p50/p90=14/40/66)/(b) 一律 30 分/(c) 0。**推奨 (a)**(唯一の個体別の値・新規取得不要。ただし意味=通勤時間か余裕時間かは未確認=リスク 5)。
4. **終業**: 第36表(帰宅・同じ雇用形態)から独立に引き、終業=帰宅−`arrival_lead_min`。勤務長を [4 h, 12 h] にクリップし `work_len_clipped` を計数(矛盾率を報告)。
5. **出勤しない日**: 第31表の行動者率で曜日ごとに引く(**D-67 (b) 0.88 と同源**=重ねるときは二重掛けにしない宣言)。
6. **修復の置換**: `duty_clamped`(本番 314,392 件)を廃し `duty_window_dev_min` の計数のみに。`duty_off_day`(272,032 件)は 5 で facts 側へ。
7. **較正と検証の分離**(第147): 投入=第31表(全国・雇用形態別)/raking の較正カーブ=W12 hour 重み(2015 PT・expedient)/判定=第30表(東京都)。判定表は下見前に事前登録。

## §3 P2 のプロンプト案 2 段(骨子)

**段1 同一性(面接形式・名前提示・3〜5 文・250〜350 字)**
- system: 「あなたは〈名前〉さん本人です。生活時間調査の面接に本人として答えます。3〜5 文・話し言葉で。」禁止 4 つ=①与えられていない固有名詞 ②与えられていない数字 ③箇条書き・見出し ④挨拶と自己紹介の定型。
- user: 事実行(名前/年齢/性別/職業/雇用形態/役職/業種/自宅の方面/出入口/**出勤 HH:MM・帰宅 HH:MM**(P1 の値)/就寝の癖/来街者は目的と頻度)+質問 3 つ(「ふだんの平日、朝は何時ごろ動き出して、どんな順で一日が進みますか」「帰りに寄るところはありますか」「休みの日は平日と何が違いますか」)。
- **指紋を最小に**: 性格・価値観・趣味・好みを**聞かない**(列挙で誘導しない)。プールの `persona` 文(作者タグ)と `traits` は**渡さない**。答えの中に自ずと出たものだけ採る(ユーザー選好)。
- 復号: 自由文・regex なし・`max_tokens=320`・温度 0.7。検査: 字数 200〜420・禁止 4 つ(固有名詞は W14 の残差方式を流用)。落ちた体は段2 を事実だけで書かせる(骸格ではない)。

**段2 週次表(本人が書くブロック形式)**
- system: 行=`HHMM-HHMM <block_kind> <活動語> <場所語>`(0 域外/1 自宅/2 在圏/3 就寝/4 移動)・**切れ目なく**(0000 始まり 2400 終わり)・**就寝行を 1 本以上**・**0〜4 時に支度/乗車を置かない**・域外居住者の自宅は `block_kind=0`。**例示の時刻を丸めない**(現行 few-shot は全部 :00=これ自体が :00 80% を教えている)。
- user: 段1 の本人の言葉をそのまま貼る→事実行→「あなたが上で話したとおりの 1 週間を表にしてください」。
- 文法: `d0..d6` 見出し+行 `{3,10}`/日・`max_tokens=1,024`。検査で弾く(修復しない・計数): 隙間/重なり・被覆 <1,440・就寝 0 分・0〜4 時の支度乗車・域外居住者の自宅が `block_kind≠0`・1 行 10 分未満・`block_kind` と場所語の矛盾。
- **8B で下見**(答申 §7-3)。32B は比較腕 200 体だけ。

## §4 標本と実行

| 種別 | 母集団 | 下見 n | 重み |
|---|---|---|---|
| 0 通勤 | 215,772 | 1,090 | 198.0 |
| 1 来街 | 58,602 | 296 | 198.0 |
| 6 定期来街 | 20,000 | 101 | 198.0 |
| 7 訪日 | 17,404 | 88 | 197.8 |
| 3 住民 | 36,545 | 185 | 197.5 |
| 5 通学 | 33,385 | 169 | 197.5 |
| 2 従業 | 7,077 | 36 | 196.6 |
| 8 乗務 | 1,258 | 25(下限) | 50.3 |
| 4 指令 | 24 | 10(下限) | 2.4 |

- 抽出=既存 `pilot_rows` と同規約(`_mix64(agent_id)` 昇順・種別ごとに上位 n)。**全腕で同じ 2,000 体**(対応のある比較)。seed ドメインに腕タグを入れる。
- 実行: 既存の fleet_gen・DP7・Qwen3-8B INT8・温度 0.7・repeat 1・thinking false。
- **費用**(艦隊実測 W17 本番 ≈20.8 呼/s): P0b 1.6 分・P1a/P1b 各 1.6〜2.0 分・P2 段1 0.8 分・段2 2.8 分・P2-32B ≈15 分 → **計 8,400 呼・約 25 分**。本番投入時: P1 ≈ 5.2 h(現行同形)・**P2 ≈ 11〜13 h**(2 段)。

## §5 指標と現実アンカー・P0 の前値(サブ実測・親は M3a を独立確認)

| ID | 定義 | **P0 前値** | 現実アンカー | 期待 |
|---|---|---|---|---|
| M1 | Schneider 定義のモチーフ(場所の同一性と辺の向き)の個数と累積被覆 | 未測定(店・駅の `target_cell` が −1) | 17 個で 90% | 上限側の門番 |
| M2 | 時刻込み署名の上位 1 型占有率 | 通勤 13.7% | 東京都出勤の最頻 15 分ビン 11.93% | 門番(単独で不合格にしない) |
| M3a | 全活動の開始分の :00+:30 率/H | **0.919**(親確認)/4.241 bit | — | ↓/↑ |
| **M3b** | **出勤代理**(勤務直前の移動/乗車の開始)の :00+:30・最頻ビン・H | 0.971・08:00 55.8%・2.408 bit | **0.675・11.93%・4.680 bit**(第30表 東京都) | **P1 の主目標** |
| M3c | 勤務開始の :00+:30・最頻・H | 0.993・09:00 66.1%・2.024 bit | 区分別 62.6〜81.8% | ↓ |
| M4a | 活動数/(体・日)平均±sd | 5.78±0.46(文法 `{4,5}` が決める) | PT の活動数分布 | sd ↑(文法を緩めた腕だけ) |
| M4b | 1 日の相異なる場所種別数 | 3.28±0.66・7 未満 100% | 対数正規・90% が 7 地点未満 | σ ↑ |
| M5 | 体ごとの S/Π^max の分布 | 7 日では測れない=報告のみ | Π^max 0.93 に鋭い | 分散が過大でも不合格 |
| **M6** | 個人内一貫性 J(d0,d)=(活動,場所)集合の Jaccard | **0.879±0.217・完全一致 73.0%** | J はべき減衰(答申 §1-5) | **P2 の主目標**(完全一致率 ↓・J を 0 にしない) |
| M7 | 同一文脈での選択分散 | 下見では測らない(C7 テープ) | 70B は 88% で p>0.999 | — |
| M8 | 馴染みの場所集合の相互 Jaccard・容量分散 | 部分測定(cell 未解決) | 容量 ≈25・中身は個別 | P2 の主目標(要 cell 解決) |

**弊害側(どの腕でも悪化させない)**: 修正率 0.1871(門番 <0.20・余裕 0.003)・JSD max 0.4211・parse 失敗 0.0042・出力 tok p90 600=上限張り付き・`duty_clamped` 314,392・`duty_off_day` 272,032・骸格率 0。

**採否の考え方(合格線は親が決める)**: ①P0 vs P0b のフロア併記 ②改善率 `(|P0−現実|−|腕−現実|)/|P0−現実|` ③合格線候補: M3b :00+:30 0.55〜0.82・H ≥4.0 bit・最頻 ≤0.20/M6 完全一致率 ≤0.20 かつ J 0.40〜0.80/M4a sd ≥1.0 ④門番: 修正率 <0.20・parse 失敗 <0.02・被覆率 ≥0.999・深夜違反 0・就寝行あり 1.0。

## §6 ゲート再定義(W17 v2)と所見

| ゲート | 案 | 現行実測 | 所見 |
|---|---|---|---|
| JSD | ≤0.05(D-23 仮置き) | 0.4211(raking 後)/0.5559(前) | 届かない。較正カーブ自体が 2015 PT の expedient。下見は `jsd_before` を主指標に・合格線は実測後 |
| 修正率 | <0.20 | 0.1871 | `duty_clamped` を検査へ移すと分子から抜ける一方、被覆埋めが新たな修正を作る→分子分母の再定義(決定項 10) |
| 被覆率 | ≥0.999(新) | 未測定(隙間だらけ=D-63) | LLM に切れ目なく書かせるか、エンジンが埋めるか |
| 深夜違反率 | 0(新) | 03:30 支度 562・乗車 141(5,000 体) | 文法から外すのが確実 |
| 就寝行あり率 | 1.000(新) | 域内居住者のみ強制(`sleep_added` 10,442) | 域外居住者にも要求(自宅=域外) |
| 自宅セル解決率 | ≥0.95(新) | 自宅活動の 84% 未解決 | M1/M8 に必要 |
| 多様性(新) | M3b・M4a・M6 の 3 本 | — | M1/M2 は門番として報告のみ |

## §7 リスク

1. 8B の類型化(面接形式の効果量は図のみ=数値なし)。2. 2 段で本番呼数 2 倍(≈11〜13 h)。3. raking は 1 時間単位の平行移動で分は保存するが **1,188,863 行(候補の 61%)を動かし M6 を縮める**方向。4. 雇用形態の按分は全国人数比の expedient。5. `arrival_lead_min` の意味未確認(出勤≠始業)。6. 文法緩和で出力 tok(p90 600=上限)・切断・parse 失敗が増える。7. 名前提示は性別・年齢の手がかりを二重に与える。8. 下見ファイル名が本番 glob に当たると `build_hash` が動く=機械検査。9. M1/M8 は `target_cell` 未解決で部分測定=合否は M3/M4/M6 で。

## §8 ユーザーに聞く決定項(候補・推奨・根拠)

| # | 論点 | 候補 | 推奨 | 根拠 |
|---|---|---|---|---|
| 1 | 雇用形態の出所 | (a) プール `employment` / (b) kind→写像 | **(a)** | 欄が実在・欠損なし(親確認) |
| 2 | 非正規の内訳 | (a) 第31表の人数比で按分 / (b) 「正規以外」1 本 | **(a)** | アルバイトの出勤 p90 17:30 は平均と大きく違う |
| 3 | 出勤→始業 | (a) `arrival_lead_min` / (b) 一律 30 分 / (c) 0 | **(a)**(意味の確認つき) | 唯一の個体別の値 |
| 4 | 同一性の長さと項目 | (a) 3〜5 文・生活の運びだけ / (b) 価値観・好みも / (c) プールの persona 文を渡す | **(a)** | 「自ずと」の選好。(b)(c) は指紋 |
| 5 | 32B 比較腕 | (a) 200 体だけ(≈15 分)/ (b) なし | **(a)** | 8B 続行の判断を 1 度だけ反証可能に |
| 6 | 週 7 日 vs 1 日 | (a) 7 日 1 呼 / (b) 1 日 1 呼 | **(a)** | M6 に 7 日が要る。(b) は本番 36 h=不成立 |
| 7 | 腕の割り方 | (a) 5 腕 / (b) 3 腕 | **(a)** | 帰属のため。追加費用 2 分 |
| 8 | 合格線の形 | (a) 改善率+フロア / (b) 絶対値 / (c) 両方(門番は絶対値) | **(c)** | 多様性は改善率・弊害は絶対値 |
| 9 | raking の予算 | (a) 12% 据置 / (b) 0% / (c) 両方を P1b で測る | **(c)** | LLM の分布と較正後を分ける。追加費用 0 |
| 10 | 修正率の分子分母 | (a) 現行 / (b) 再定義 / (c) LLM 由来とエンジン由来を別々に報告 | **(c)** | 前後比較を切らない |
| 11 | JSD ゲート | (a) 0.05 のまま / (b) 実測後に決める / (c) 判定を第30表へ | **(b)+(c)** | 較正カーブが expedient。第147 と整合 |
