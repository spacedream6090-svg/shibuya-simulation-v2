# 記憶の実装アジェンダ v0(2026-09-17・第231・親起草・ユーザー Q&A 待ち)

> 位置づけ: ユーザー「記憶などの実装はまだ始められないのか」(第229・D-95)への答え。CLAUDE.md §2-2(詳細まで確定してから実装)に従い、[v2-memory-brief.md](v2-memory-brief.md)(材料集め・472 行・親が事実 3 件をコードと予算表で確認)の §5 の 16 問を、**親推奨つきの決定項**に組み直したもの。決定済みの式(認知設計 §4: score=w_r·A+w_i·importance+w_v·relevance・A=ln Σ(Δt+1)^−d・d=0.5/0.25)は変えない。答えは M1〜M16 の番号で。「推奨で」一括可。

## 0. 実装前に知っておく事実 3 件(親確認済み)

1. **日次内省はいま本番で 1 回も発火していない**。D-62(既定 `plan_sleep=True`)で就寝の計画境界は LLM を呼ばず、`n_reflections` を増やす経路は行動語「就寝」だけ。c7-day-4 のテープ 13 語に「就寝」は無い。文面(「眠りにつく時刻になったため、一日をふりかえります。」)だけが templates.py に在る。
2. **T2 内省を L2=2,048 tok で出すと壁時計が 78.4 h/日**(L5 42 万呼 × 2,048 tok=8.6 億 tok ÷ 実測 3,046.9 tok/s)。現行 9.17 h の 8.5 倍で W1 ≤24 h を単独で壊す。o128 なら +4.9 h・通常呼並みの 37 tok なら +1.4 h。**分岐点は呼数でなく出力トークン長**。
3. **SoA に欄を足すと既定腕の checkpoint が動く**(`Registry.state_hash` は宣言順の全配列を混ぜる)。C9a/C9b と同じ「腕でだけ確保する欄」(既定 OFF)にしないと退化検査(ba01bd0b ほか)が壊れる。M1 予算は既定 127 B/体・上限 30,000 B/体で余裕あり(構造化 3.0 KB を足しても 21%)。

加えて: 記憶行は観測ブロック B5/B6 にしか置けない(B0〜B4b は個体依存語を毎回検査)。個体枠 300 tok の実測平均 133.3 で残り 166.7 tok。観測に 1 行足すと `prompt_hash` が変わり既存テープは全行 miss(新版として扱う)。会話の発話本文はいま捨てている(テープの `response` 列にだけ残る)。事象の供給量は約 8.9 エピソード/体/日(呼 7.0+気づき 1.9)。

## 1. 決定項(M1〜M16)

| # | 決めること | 選択肢 | **親推奨** | 根拠・注意 |
|---|---|---|---|---|
| **M1** | エピソードのレコード型と粒度 | (a) エンジン事象 ID+tick+場所+相手+対象+結果コード+重要度(自由文なし)/ (b) (a)+理由 40 字 / (c) 呼 1 件=1 本 / (d) 成否が確定した行動だけ | **(a) を基本、会話エピソードだけ (b)**(発話の要旨 40 字を持つ) | エンジン事象は ID で引けるので埋め込み不要(答申 4-4)。会話本文は現在捨てているので、会話だけ自由文を残す。1 件 ≈ 24 B(会話は +40 字) |
| **M2** | 保持数と忘却 | (a) 直近 N 件の環状 / (b) A(m) の下限で落とす / (c) 内省で自由文だけ落とし構造化は残す | **(b)+(c)、上限 N=128 件(構造化 3.0 KB)** | 8.9 件/日なら 14 日で満杯=忘却規則は必須。d=0.5 は既決。N は expedient(感度 64/256) |
| **M3** | importance=S(e) を先に作るか | (a) S(e) 5 項をエンジン実装 / (b) 当面は結果コード由来の固定表 / (c) 重要度なし | **(b)**(失敗>成功・会話>単独・初回>反復・被注視あり>なし の 4 段) | S(e) は src に 0 件で w1..w5 が全部 expedient になる。固定表なら指紋は 1 表で宣言できる |
| **M4** | 想起の呼び出し点 | (a) 起床クラスごとに k 件 / (b) 全起床で一律 / (c) 内省呼だけ | **(a): 会話 k=3・計画境界 k=2・個体/セル起床 0。B5 に「記憶」チャネル 60 tok を新設(個体枠 300 の残り 166 tok 内・他チャネルは削らない)** | 固定枠 vs 単一ランキングは既に義務 ablation(D-60)。記憶行の追加は文面の版上げ=新版テープ |
| **M5** | 想起のクエリ q | (a) 起床理由+現在セル+直前の結果 / (b) 観測全文 / (c) 「いま何をしますか」 | **(a)** | 決定論と安さ。埋め込み無しなら q は ID 一致の鍵(セル・相手・対象)になる |
| **M6** | 埋め込みを使うか | (a) 第1段は無し(ID 一致+A(m)+importance)/ (b) 最初から Ruri-v3-30m / (c) 会話要約だけ | **(a)。Ruri は腕として後**(スループット未計測・huggingface.co は §7 の例外に無い=取得はユーザー判断) | relevance 項は w_v=0 で始め、腕で入れて効きを測る |
| **M7** | 内省の発火口 | (a) 就寝実行の直後に内省呼 1 本(+0〜30 分ジッタ)/ (b) 行動語「就寝」を LLM に戻す / (c) 独立の内省起床クラス | **(a)** | 知覚契約 §6 の決定と一致。D-62 の腕を崩さない。上位 5% の即時内省は第2段 |
| **M8** | 内省のモデルと出力長 | (a) 同じ 8B・o128 / (b) 8B・2,048 tok / (c) 別モデル同居 / (d) 出力を「要旨 最大 3 × 1〜2 文」で縛る | **(d)+(a)**(8B・最大 128 tok・要旨 3 本) | (b) は W1 超過。別モデルは R16 とハーネス研究(R-35)の後。品質プローブで 8B の recall 0.833・幻覚 0 |
| **M9** | 内省の入力 | (a) その日の全エピソード時刻順 / (b) 重要度上位 k / (c) `[B5 記録]` 5 行形(前例) | **(c) の形で (b) 上位 k=12 件・入力 ≤1,300 tok** | 前例が実測つきでリポ内にある(tools/quality_probe_v0) |
| **M10** | gist の出力契約と parser | (a) 内省専用の行形+専用 parser / (b) 既存の寛容 parser / (c) 同じ呼で「関係: ±1・気分: 語」も取る | **(a)。気分は語 1 つだけ許す。関係の ±1 は書かせない**(D-93 (b)) | 8B は数値尺度で壊れる(FR 46.22%)。書式エラー受入 ≤0.10 を内省呼にも課す |
| **M11** | 再生に何を書くか | (a) 内省呼を PLAN_BOUNDARY で書く / (b) EventClass に 1 値足す / (c) 想起した記憶 ID を列に足す / (d) 書かない | **(b)+(c)**(末尾追加で class_rank 不変を確認・テープ版 3) | 「なぜその記憶が出たか」を再生で再構成できるのが検証の要 |
| **M12** | D-93 関係辺を記憶から導く | (a) 辺の強さ=相互作用エピソードの A(m)・好悪は ResultCode / (b) 会話応答の 3 値 / (c) 元の指紋 3 | **(a)**(第230 の推奨と同じ) | Gilbert 2009・Zhao 2012(親確認) |
| **M13** | 習慣(T0)と訪問履歴 | (a) 訪問カウンタだけ先(K-1 (a))/ (b) 馴染みの場所 上位 k を観測へ / (c) T0 スロット表まで | **(a)。(b) は腕**(D-81 の条件どおり) | 同じエピソード記録から数えられる=追加の欄は訪問回数だけ |
| **M14** | ablation の対応表 | 3 つの宣言(認知 §7 A/B/C・知覚 §8 ⑤・S4 記憶あり/なし)を 1 枚に | **畳む**: 記憶なし / 記憶あり・内省 1.05/日 / 内省 2〜3/日 / 想起なし内省のみ / persona 可変。口は `introspection_per_day`(AB5 の提案名) | L2 の 20% 予約と S4 の 128 h を 1 表で管理 |
| **M15** | 記憶の合格線 | (a) 内部ゲート(一貫性・出力長)/ (b) 台帳の holdout 行 / (c) K-2〜K-4 の内部指標 | **(c)+(b)**: K-2(同一モチーフの再出現 ≥ 無作為期待の 10 倍)・K-3・K-4(MC(1) 0.87〜0.95 の下限側)を内部門番、台帳行は封印して事前登録 v2.0 へ | 複数日ランが前提(1 日では測れない) |
| **M16** | 検索閾値 τ | (a) 固定+感度腕 / (b) 自己調整 / (c) 上位 k で切る | **(a)**(Zhao 2012 準拠・τ は A1/B3/B4 に較正)。(b) は未リサーチ | τ が網の大きさを決める(800 vs 1,336)=感度必須 |

## 2. 実装の段(推奨・各段に切替口=既定 OFF・checkpoint 不変・テスト・モック 5,000 → 実 LLM スモーク)

1. **段1 記録と想起(埋め込み無し)**: エピソード SoA(腕でだけ確保)・A(m)・importance 固定表・訪問カウンタ・想起 k 件を B5「記憶」チャネルへ・テープ版 3(想起 ID 列)。**内省なし**でも「馴染みの場所」と「直前の失敗の想起」が出る。
2. **段2 内省**: 就寝直後の 1 呼(8B・128 tok・要旨 3 本)・専用 parser・EventClass 追加・`introspection_per_day` の口・ablation 対応表。
3. **段3 接続**: 関係辺(C10・D-93)と習慣(K-1)を同じエピソード記録から導く。τ の感度腕。合格線 K-2〜K-4 を複数日ランで。

## 3. 触れる決定項・予算(宣言)

M4(記憶 6.1 KB/体=既定 127 B の外・腕でだけ)・M11(B5 +60 tok)・L5(42 万呼/日=1.05/体/日)・L7(出力 128 tok に縛る=+4.9 h・W1 内)・S1(テープ +1 列)・P4(想起は O(k) の配列演算・逐次ループなし)。既定 4 種 checkpoint は不変が条件。
