# C11 記憶(エピソード記憶・想起・日次内省)— 設計ラウンドの材料(事実の棚卸し・2026-09-17)

> **位置づけ: 材料のみ。決定も推奨も書かない**(親がこれを元に決定アジェンダを組む)。
> 出典は `ファイル:行` / 決定台帳の行 / PENDING の番号で示す。親が確かめていない推測には **[推測]**、
> 本調査でリポ内資産から再計算した値には **[再計算]** を付す。Web・サーバーは使っていない。
> **行番号の注**: HEAD `9413776`(第229)+作業木。`perception/renderer.py` / `engine/run.py` /
> `engine/llm_bridge.py` / `cli.py` / `build/geo/` は第229 の実装サブ(D-59・W6)が**同時に編集中**なので、
> 行番号は動きうる。**記号名(関数名・定数名)のほうが安定した参照**として読むこと。
> 対象工程: S3 の **C10 関係+記憶** / **C11**(`docs/design/v2-schedule-and-research-plan-2026-09.md:40`
> =「関係辺・エピソード・**日次内省 L5=42 万呼/日**」・出口ゲート「待ち合わせ成立 / 会話 ≫ 410/日 /
> **L5 予算内** / 決定論再生一致」。同 :41 の S4 は「**7 シミュ日ラン × 2 腕(記憶あり/なし)**」)。
> **C10 との境界**(`docs/design/v2-c10-relations-agenda.md`): 関係辺・約束・会話の起点は C10。
> 起票: **PENDING D-95**「記憶の実装をいつ始めるか」(第229・ユーザー「記憶などの実装はまだ始められないのか」・`PENDING.md:104`)。
> 本書が扱うのは「何を覚えるか」「いつ思い出すか」「夜に何を書くか」。ただし **D-93 で境界が動いた**
> =「辺の強さを記憶の基底活性から導く」案(`PENDING.md:102`)により、**記憶の実装が C10 の着手条件の前提**になった。

---

## §1 いまの表し方(事実)

### 1.1 体が「覚えている」もの = SoA の **4 欄・7 バイト**だけ

`src/shibuya/agents/state.py:447-456`(「直前の結果」・行動契約書 §6):

| 欄 | dtype | B | 意味 |
|---|---|---|---|
| `last_action` | int8 | 1 | 直前に**試みた**行動コード。`-1`=`LAST_ACTION_NONE`。**エンジン継続(`ENGINE_STEP`)は書かない** |
| `last_result` | int8 | 1 | `ResultCode`(0=OK・1〜20 が失敗の型) |
| `last_result_tick` | int32 | 4 | `last_result` を書いた tick(古い失敗を出さないため) |
| `fail_streak` | uint8 | 1 | 同一失敗の連続回数(§6「4 回で打ち切り」・expedient) |

これ以外に「過去」を持つ欄は **無い**。`activity`(現在の状態)・`plan_cursor`(週次表の消化位置)・
`poi_since`/`queue_since`/`board_since`(いま在席/並び/待ちを始めた tick)は現在の状態であって履歴ではない。

- **SoA 全体の宣言バイト**(`Registry.budget_report()` を本調査で実行)[再計算]:
  **既定 127 B/体(36 欄)** / `plan_columns` つき 129 B(38 欄) / `edge`+`attention`+`plan` 全部で 142 B(42 欄)。
  上限は **M1 30,000 B/体**(`src/shibuya/core/soa.py:73` が予算表 M1 を読む)。
- `state.py:6-8` が明記:「本モジュールが確保するのは M2+M12 相当の**コア欄のみ**
  (T0習慣 M3・**記憶 M4**・関係 M5・プロフィール M6 は **C3 以降の別レジストリ**)」。**その別レジストリは存在しない**。

### 1.2 「持っていない」ことを確認したもの(grep で 0 件)

| 探した語 | `src/` + `tools/` の結果 |
|---|---|
| `内省` | `resolve.py:39`/`:1863`(docstring「発火を**記録するだけ**」)・`contract.py:322`(行動語「就寝」の効果文)・`templates.py:470`(コメント)のみ。**内省の呼を出すコードは無い** |
| `reflection` / `episodic` / `memory_` | `ResolveOutcome.n_reflections`(計数 1 本)・`DIAG_RUN_COLUMNS` の `"reflections"` 列のみ |
| `ruri` / `embedding` / `埋め込み` | `src/` に **0 件**(`build/audit/w19_freeze.py:18` はヘッダ埋め込みの別義)。`pyproject.toml:12-24` の依存は numpy/numba/pydantic/pyyaml/blake3/xxhash/cbor2/pyarrow/duckdb/zstandard/httpx の **11 本だけ**=`onnxruntime`・`sentence-transformers` は入っていない |
| `想起` / `recall` / `retriev` | `templates.py:14`(コメント)のみ |
| `訪問` / `visit_count` / `visits` | **0 件**(**K-1 の訪問履歴カウンタは無い**・`PENDING.md:92` D-81) |
| `忘却` / `Ebbinghaus` | **0 件**(監査 B が同じ確認をしている: `docs/research/v2-research-reflection-audit-p1p2.md:178` 表「`src/` に `忘却`・`Ebbinghaus` ゼロ」) |
| `習慣表`(T0) | 無い(同上「習慣表(T0)が `src/` に無い」) |

### 1.3 観測ブロック B0〜B6 に入っているもの(どこに記憶が入りうるか)

契約の枠(`src/shibuya/perception/templates.py:104-127`・知覚契約書 §2.2):

| # | ブロック | 予算 tok | グループ | 実装の中身(`perception/renderer.py`) |
|---|---|---:|---|---|
| B0 | system/役割テンプレ | 300 | shared_static | 憲法・役割知識・出力規約(`B0_SYSTEM` / `_OPEN` / `_HINT` / `_V2`) |
| B1 | 種別 | 100 | shared_static | 「あなたは{kind}です。」の 1 行だけ(年代・性別は §2.4 ⑧ で除去済み) |
| B2 | 場所セル静的 | 150 | cell | 現在地・路面・可視物・看板・目印 |
| B3 | 時間帯・天候 | 100 | shared_static | 時刻(5 分丸め)・天候/日照・暑さ |
| B4 | 場所セル動的 | 60 | cell | 密度段階+流れ・騒音段階・目につく出来事 |
| B4b | サブセル動的 | 40 | cell | 「25 メートル以内: …」 |
| **B5** | **個体固有** | **220** | **individual** | 内受容(閾値割れ分)・**所持金/手**・**直近の行動 1 語**・近接人物 上位k+知人・被注視・(傍受・広告) |
| **B6** | **起床理由・直前の結果・問い** | **80** | **individual** | 起床理由 1 行・`B6.result_ok`/`result_fail`/`result_none` 1 行・「いま何をしますか。」 |

- **グループ上限は実行時に強制**される: `Renderer.render`(`renderer.py:879-884`)が `shared_static 750 / cell 250 / **individual 300**`
  (`templates.py:123-127`)を超えると `ValueError`。トークン見積りは **UTF-8 文字数 ÷ 2**
  (`perception/channels.py:73-78`・expedient)なので、**個体枠 300 tok ≒ 日本語 600 字**。
- **c7-day-4(390,067 体・実 LLM)の実測平均**: 共有静的 **642.8/750**・セル **147.3/250**・
  **個体 133.3/300**(合計 923.5)・切り詰め 83 件(`docs/bench/c7/accept_c7-day-4/run_cli_summary.txt`)。
  **個体枠の平均残りは 166.7 tok ≒ 333 字**[再計算]。
- **B0〜B4b には個体依存語を一切入れられない**: `Renderer.render`(`renderer.py:869`)が毎回
  `N.assert_no_agent_dependent_words(shared)` を走らせる(§2.4 ⑧)。**記憶は B5 か B6 にしか置けない**。
- **B5/B6 に入っていないもの**(本調査の grep): **ペルソナ文・同一性文・週次表(自分の予定)・
  訪問履歴・過去の会話・知人名**。`persona`/`同一性`/`identity` は `src/shibuya/perception/` と
  `engine/run.py` に **0 件**で、同一性文は `src/shibuya/build/sched/trial.py`(W17 生成時・オフライン)にしかない
  =`pyproject.toml:82-89` の層契約で **`build` は実行時に import されない**。
  **体は自分の予定も来歴も見ていない**(エンジンが `presence` で計画を実行する)。
- 知人は口だけある: `Renderer._nearby_items`(`renderer.py:1224` 付近)が `P-<id>(知人)` を常掲するが、
  `Renderer(acquaintances=)` を **本番は渡していない**([再計算] `engine/run.py` に `acquaintances` は **0 件**・
  `v2-c10-relations-brief.md` §1.2)= 全員「未知」。

### 1.4 テープと checkpoint に残るもの(再生の材料)

- **テープ = 1 呼 1 行 Parquet/zstd・版2 `shibuya.tape/2`**(`src/shibuya/engine/tape.py:88-122`)。
  列は **13**: `call_id / agent_id / tick / wake_class / prompt_hash / block_ids / params_hash /
  response / tokens_in / tokens_out / deferred / deferred_reason / observed_tick`。
  共有ブロックは `blocks.parquet`(`block_id = blake3(text)` 先頭 16 B)に intern。
- 再生は **(agent_id, tick, wake_class, prompt_hash) 完全一致**で引く(運用設計書 §2.5)。
  → **観測に 1 行足すと `prompt_hash` が変わり、既存テープは全行 miss になる**(c7-day-2 の `tape_miss 98.7%` が先例・D-58)。
- `response` に **LLM の生出力(理由 40 字+行動/対象/ひと言)がそのまま残る**=**エピソードの自由文の唯一の在処**。
- **checkpoint** は `Checkpoint(tick, agents_hash, world_hash, population_hash, schedule_hash)` の
  ハッシュだけ(`engine/run.py:254` `class Checkpoint`)。`agents_hash = Registry.state_hash()` は**宣言順の全配列を混ぜる**ので、
  **SoA に欄を 1 本足すと既定腕の checkpoint が変わる**(`state.py:319`)。既存の腕の値は
  既定 `ba01bd0b` / v2.1 `4fb0f2ec` / vocab v2 `9c16f77a` / edge `9a9049f5` / edge+v2 `6d6b1cda` /
  plateau `1c8370fb`(`IMPLEMENTED.md` #27/#28/#32/#33)。
  → C9a/C9b/C9c と同じく **`edge_columns` 型の「腕でだけ確保する欄」**にしないと退化検査が壊れる。

### 1.5 日次内省の口: 「発火を数える」だけ・**しかも本番では発火しない**

- `engine/resolve.py:1863` `_apply_sleep` の docstring:「就寝: 当該セルが就寝可 → 睡眠状態 +
  **T2 日次内省の発火を記録**」。実体は `out.n_reflections += int(win.size)`(`:1890`)の **計数 1 行だけ**。
- しかし **D-62(既定 `plan_sleep=True`)で就寝境界は LLM を呼ばない**: `engine/run.py:1739-1745` が
  `p_cond == WakeCondition.PLAN_SLEEPING` の候補を起床候補から**外して** `presence.step_plan_boundaries` /
  `begin_planned_sleep` に渡す。`begin_planned_sleep` と `engine/presence.py` は `n_reflections` を**触らない**
  [再計算: grep で 0 件]。
  → **n_reflections が増えるのは「LLM が行動語『就寝』を選んだとき」だけ**で、
  c7-day-4 のテープには行動語 **`就寝` が 1 件も無い**(13 語の内訳に不在・`tape_stats.json`)= **実質 0**[再計算]。
- 起床理由の文面は既に用意されている: `templates.py:474`
  「**眠りにつく時刻になったため、一日をふりかえります。**」(`WAKE_REASON_TEXT[1]`)。**使われる経路が無い**。
- 不応期表の `PLAN_SLEEPING` は **360 分**(`state.py:258`・「起床まで≥360 分・内省 1 回のみ」)。
- 診断列 `reflections` は `DIAG_RUN_COLUMNS`(`run.py:179`)に在り、tick ごとに書かれる。

### 1.6 コードに在る「記憶らしきもの」3 件(いずれもエピソード記憶ではない)

| 実装 | 値 | 出所 |
|---|---|---|
| 会話の**拒否履歴** | `REFUSAL_MEMORY_TICKS = 60`(ペアごと・`self._refusal_until[self._pair(a,b)]`) | `engine/conversation.py:90`/`:559`。知覚契約 §6「知人出現: 同一相手 60 分」からの**転用**=根拠のある値ではない |
| **広告の記憶側上界** | `MEMORY_CAP_RECOGNITION = 0.40` / `MEMORY_CAP_RECALL = 0.08` | `perception/attention.py:120-121`(知覚契約 §4 段3・VR 実験 n=41・非査読) |
| **注意の焦点の寿命** | `focus_ttl`(uint8・既定 `FOCUS_TTL_TICKS`・取得 20 m / 喪失 30 m) | `state.py:363-370`(C9b・**expedient=値の出所は無い**) |

### 1.7 会話の中身は **捨てている**

- `Session`(`engine/conversation.py:139-171`)が持つのは `session_id / participants / cell / state /
  opened_tick / speaker_index / calls / max_turns_of / **topics(本数だけ)** / last_utterance_tick /
  blocks / interrupt_queue / closed_tick / close_reason`。**要約も逐語も無い**。
- `ConversationManager.utterance(..., comment="")` は「ひと言」欄を**引数で受け取るが本文で一度も使っていない**
  [再計算: `conversation.py` 中の `comment` の出現は :612 の引数と :620 の docstring の 2 箇所のみ]。
  発話本文はテープの `response` にしか残らない。
- 行動契約書 §4「記憶転写の規則」(宛先=全文 1.0 / 非宛先=要約 0.5 / 傍受 0.2・**個体で可変**)は **未実装**
  (`docs/design/v2-implementation-plan.md:102`「C2 未実装: 会話の継続・終了・**記憶転写**・**関係辺**」)。

---

## §2 使えるデータ(事実)

### 2.1 記憶の中身になりうるエンジン事象と 1 シミュ日の件数

**c7-day-4**(390,067 体 × 1,440 tick・実 LLM 8B INT8・温度 0.7・`docs/bench/c7/accept_c7-day-4/`):

| 事象 | 件数/シミュ日 | 1 体あたり[再計算] | 出所 |
|---|---:|---:|---|
| **LLM 呼(=起床)** | **2,706,593** | 7.01(呼んだ体 386,078) | `tape_stats.json` `rows` / `agents_distinct` |
| ├ 会話クラス(0) | 300,098 | | `tape_stats.json` `wake_class` |
| ├ 計画境界(1) | 1,201,143 | | 同上 |
| ├ 個体変化(2) | 561,458 | | 同上 |
| └ セル変化(3) | 643,894 | | 同上 |
| 行動「移動」 | 1,421,317(52.51%) | | `tape_stats.json` `actions` |
| 行動「購入」 | 1,048,864(38.75%) | | 同上 |
| 行動「休憩 / 乗車 / 待機」 | 109,115 / 48,019 / 43,770 | | 同上 |
| 行動「(未定義)」 | 29,041(1.07%) | | 同上・上位語 探索 8,710・観察 2,830・近づく 672・通勤 613 |
| 行動「会話」 | 4,040(0.149%) | | 同上 |
| 行動「退去 / 通報 / 手伝い / 並ぶ / 降車 / 断る」 | 1,133 / 640 / 468 / 152 / 23 / 11 | | 同上 |
| **顕著行為に気づいた延べ人数** | **729,683** | **1.89** | `run_cli_summary.txt`「顕著行為 228(**気づき 729,683**・出動 114)」・`engine/processes/salient.py:253` `n_noticed` |
| 顕著行為そのもの | 228 件(倒れる/police/broadcast の 3 クラス) | | `salient.py:71-88`(`D50_BY_KIND` 40/45/60 m) |
| 会話セッション成立 | **410** | 0.001 | `run_cli_summary.txt` `conversation_sessions: 410` |
| 乗車 / 乗車待ち / 打ち切り / 乗り残し | 36,434 / 37,392 / 185 / 0 | | 同上 |
| 待ち行列 | 7,712 | | 同上 |
| 店舗売上(=購入の成立額) | 19,428,800 円 | | 同上(**成立件数は印字されていない=空欄**) |
| 補充 / 納品 / 宅配 / ホテル泊 | 140 / 2,337 / 6,997 / 448 | | 同上 |
| 計画就寝(その場+着いて) / 計画起床 | 3,005+1,627 / 41,978 | | 同上 |
| 未定義行動(台帳に載った) | 1,178 | | 同上 `undefined_action_count` |
| 繰り延べ / 昇格 / 縮退 / 抑止 | 2,400,061 / 1,492,972 / 992,389 / 3,765,119 | | 同上 診断 4 列 |

- **1 体 1 日あたりの「事象」候補は 呼 7.01 + 気づき 1.89 ≈ 8.9 件**[再計算]。30 シミュ日なら 267 件。
- **失敗コード別の件数は受入記録に無い=空欄**。別ランの参考値: AB7c(5,000 体・語彙 v2)で
  **食事の選択 16,327 に対し成立 7,024(43%)**(`docs/bench/c8/ablation/ab7c_s1_parent_report.md` §1)。

### 2.2 事象に既に付いている ID(埋め込み無しで引ける材料)

- 場所: `cell`(place_id・520 個)・`node`(歩行グラフ 3,499)・`poi_ref`・(C9a で `edge_id`/`edge_s`)。
- 人: `agent_id`(`P-<id>` の表層)・`talk_partner`・(C9b で `focus_target`=符号で人/POI を分ける)。
- 物: `TargetKind.ITEM_CATEGORY`(c7-day-4 で 2,401,172 行)・`STATION_OR_VEHICLE`(5,313)・
  `PERSON`(3,326)・`CELL`(33)・`NONE`(296,749)。
- 結果: `ResultCode` **21 値**(0=OK〜20=`TARGET_GONE`・`state.py:184-204`)+ 文言表 `RESULT_TEXT`。
- 起床: `WakeCondition` **11 値**+ `EventClass` **5 値**(`core/types.py:99-114`)。
- 重要度: **`S(e)` は未実装**(`grep` で `surprise`/`stake`/`novelty` は `src/` に無い)。
  認知設計書 §3 は `S(e)=w1·surprise+w2·stake+w3·novelty+w4·recent_failures+w5·social_exposure`
  を「全項エンジン算出・LLM 採点なし」と決めている(`v2-cognition-design.md:40`)。
  → **`importance = S(e) の離散化` は、S(e) の実装とセットでないと作れない**。

### 2.3 自由文の出所(いま捨てているもの)

| 自由文 | 長さ | いまの行き先 |
|---|---|---|
| 「理由: 」(1 行目) | **40 字以内**(超過は切り詰め・`parser.py:16`) | テープの `response` のみ。SoA にもログにも入らない |
| 「ひと言: 」(2 行目) | **20 字以内** | 同上(`conversation.utterance` は受け取るが使わない=§1.7) |
| 会話の相槌・締め | 固定文(`BACKCHANNELS`・`"それじゃ、また。"`) | エンジン生成・文面凍結 |
| 看板(B2.signage) | W14 凍結文 | 観測に出るだけ |
| 未定義行動の逐語 | `raw_action` | `tools/vocab/registry_from_tape.py` が台帳へ(D-71 段1) |

### 2.4 埋め込みモデル(答申の記述・**親の一次確認は「モデルカード実読」まで**)

- **cl-nagoya/ruri-v3-30m**(`docs/research/v2-cognition-detail-research.md:310-316`・
  「モデルカード実読 `https://huggingface.co/cl-nagoya/ruri-v3-30m`」・同 :491 で ✔ 実読):
  **37M パラメータ(埋め込み除くと 10M)・出力 256 次元・最大系列 8,192・JMTEB 平均 74.51・Apache-2.0**。
  上位: v3-70m 75.48 / 130m(512 次元)76.55 / 310m(768 次元)77.24。
  **プレフィックス必須**: 検索は `"検索クエリ: "` / `"検索文書: "`(書き込みと読み出しで固定)。
- **既存答申の「Ruri-small が優位」は第203 で訂正されて消えた**:
  `docs/research/v2-memory-retrieval-research.md:36-37`
  「Ruri-small(68M・768 次元)の JMTEB **71.53 は mE5-large 71.65 と同等(超えていない)**…
  旧記載『mE5-large(70.90)超』『mE5-small=67.71』はいずれも誤り」。
  監査 B(`v2-research-reflection-audit-p1p2.md:178` 表末行)は この項を **「未採用・理由の記録あり」** と判定。
- **スループットは未計測=採用条件**(認知設計書 §6 の登録簿行「Ruri-v3-30m のスループット | 未計測 |
  CPU/ONNX 実測=採用条件」・`v2-cognition-design.md:73`)。
- **入手の条件**: 本調査の範囲では `src/` に依存も重みも無い(§1.2)。
  CLAUDE.md §7 は「**Web からのファイルダウンロード実行はしない(読むだけ)**。例外=政府・公的機関ドメイン」
  と定めており、**huggingface.co は例外リストに無い**。実装計画書 §3 は
  「埋め込み | sentence-transformers+onnxruntime(CPU)| 固定 | Apache-2.0 | Ruri-v3-30m・
  `intra_op_num_threads` 明示+コア割当」(`v2-implementation-plan.md:23`)、
  プロセス構成は「**Proc C 埋め込み worker(CPU・スレッド数明示)**」(同 :52)と既に書いてある。

### 2.5 内省プロンプトの前例と、8B での実測(リポ内にある)

- **品質プローブ v0 の系統④「T2 内省」6 問**(`tools/quality_probe_v0/build_probes.py:508-560`)。
  文面の形(**既に B ブロック形式**):
  `[B1 種別] あなたは通勤者(30代・男性)です。` /
  `[B3 時刻] 現在時刻は23時40分です。**就寝前の振り返りの時間です。**` /
  `[B5 記録] 〔素性: 自分の行動記録・世界内データ〕**今日あった出来事の記録は次の全部です。**` +
  「08時05分 セルC-0043(宮益坂)を通って勤務先へ移動した。人出は段階Dだった。」等 **5 行**、
  問い=「今日一日を振り返ってください。」
- **実測**(`docs/bench/quality_probe_v0/summary.md:118-119`・`:148-151`):
  **`hallucination_rate = 0.000`(8B / 14B / 32B の 3 モデルとも)**・
  `recall_rate` = 8B **0.833** / 14B 0.944 / 32B 0.944。
  ただし「検出は `P-\d{3}` / `C-\d{4}` / 金額 のパターンに限られ、**『存在しない出来事の描写』型は拾えない**」。
- 参考(記憶「風」の観測行の前例): `build_probes.py:785-789` `MEMORY_LINES`
  =「直近24時間のうち、あなたはこの街路に来たことがあります。道順は分かります。」ほか **3 行**
  (`build_f1_variants.py:104` が `t["memory"]` 本数だけ差し込む腕を持つ)。**本番の描画経路には無い**。

---

## §3 決定済み・未決(事実)

### 3.1 決定済み(変えるなら delta と感度試験が要る行)

| 項 | 決定内容 | 出所 | タグ |
|---|---|---|---|
| U1 記憶と想起 | **(c) ハイブリッド**: T0/T1=構造化スロット・T2 夜間監査が統合を兼ね**昇格分だけ埋め込む**。統合前の生イベントの意味的想起は**失う(宣言して粗くする)** | `v2-redesign.md:392`(DECIDED 仮・09-01・**根拠等級 D=出典 URL なし**) | — |
| U2 習慣の初期値 | ペルソナ seed + expedient タグ + **由来フラグ**(persona 由来/自書由来)+ seed 無し ablation 可能形 | `v2-redesign.md:393` | expedient |
| 採点式 | `score(m|q) = w_r·A(m) + w_i·importance(m) + w_v·relevance(m|q)`・**w=(1,1,1) 初期** | `v2-cognition-design.md:51` | **expedient**(GA 踏襲・第2陣 ablation) |
| 基底活性 | `A(m) = ln( Σ_j (Δt_j+1)^(−d) )`(ACT-R base-level・Δt_j は**想起履歴**からの経過時間) | 同 :52 / `v2-cognition-detail-research.md:276` | mechanism(形)/ expedient(値) |
| 減衰指数 | **d = 0.5(生エピソード)/ 0.25(T2 統合済み gist = 24h ブースト)** | 同 :53 | ブースト量は expedient(`§6` 登録簿) |
| importance | **`S(e)` の離散化**(LLM 採点なし=呼数 0) | 同 :54 | S(e) の w1..w5・θ は expedient |
| relevance | `cos(Ruri-v3-30m 埋め込み)`・**自由文由来の記憶のみ**。エンジン生成事象は **ID 一致で引く** | 同 :55 / 答申 4-4「埋め込みが要るのは ①会話の要約 ②噂・口コミ ③広告/掲示の文言 ④自書の内省文」 | mechanism(切り分け) |
| バイト予算 | **M4 ≤6.1 KB/体**(構造化 3.0 KB + **12 本 × 260 B**=256 次元 int8+4 B)・40 万体 2.4 GB | 同 :59 / `v2-budget-declaration.md:36` | — |
| 日次内省 | **就寝同期・最大 3 gist/晩・ヘッダ(ID 群)+自由文 1-2 文・24h ブースト付与・生イベントは構造化のまま保持し自由文だけ落とす** | 同 :60 / 答申 4-5 の「出力契約」4 項 | — |
| 発火の規則 | **その個体の睡眠状態への遷移時+個体 ID ハッシュで 0〜30 分のジッタ**。追加トリガ=当日の重要度和が**上位約 5%** の個体のみ就寝前に即時内省(上限 1 回/日)。**固定時刻は不採用** | `v2-perception-contract.md:121` | — |
| 呼数 | **L5 = 42 万呼/日 = L4 の 10.5%(1.05 回/体/日)** | `v2-budget-declaration.md:61` | 1.05 回/日 は **ablation 第1陣⑤** |
| 索引 | **作らない**(per-agent 8-16 件の全走査=µs 級・ベクタ DB 不要) | `v2-memory-retrieval-research.md:18` | — |
| 実行場所 | **GPU は LLM 専用・埋め込みは CPU/ONNX int8 の別プロセス** | `v2-cognition-design.md:58` / 実装計画書 :52 | — |
| ペルソナ 3 層 | L2 速変数に「内受容3変数・**エピソード記憶**・**日次内省 gist**・信念/態度・選好・関係辺の強度・直前の結果」。**persona 文は不変**・変化は**近況 gist 最大 2 本**(日次内省の内数・**予算増ゼロ**)+関係辺+習慣表+選好ベクトル | `v2-cognition-design.md:82-83` | ドリフト予算あり(:84) |
| ablation A/B/C | **A=推奨案 / B=全凍結(下限)/ C=persona 文を日次で書き換え(上限)**。判定=移動分布・経済フロー・情報伝播+holdout 形状 5 指標 | 同 :85 | 初回実装に含める |
| 読解時間 | **δ_read は新着情報にのみ課金**・常在の観測プレフィクスには課さない | 同 :63 | — |
| δ_think | L2(T2 通常熟慮)= **2,048 tok = 82 秒**(`LANE_SECONDS["L2"]=82.0`・`engine/llm_bridge.py:150`。**ランは `lane` を 1 本しか取らない**=`run_day(lane=DEFAULT_LANE="L1")`・呼ごとのレーン選択は未実装) | 同 :14 | **expedient**(ablation {1,024/2,048/4,096}) |

### 3.2 未決(= 本ラウンドで決める候補。**設計書のどこにも値が無い**ことを確認した項)

1. **エピソードの定義と粒度**: 「1 呼 = 1 エピソード」か「行動の成否 = 1 エピソード」か
   「気づき・購入・会話を別種にするか」。設計書は `gist` の形(ヘッダ+自由文 1-2 文)だけを決めており、
   **生エピソードのレコード型を決めていない**。
2. **1 体あたりの保持数と忘却の実装形**: 「12 本」は**統合ベクタの本数**であって生エピソードの本数ではない。
   構造化 3.0 KB に何件入るか(= 1 件あたりのバイト)が未定。`A(m)` の下限で落とすのか FIFO か未定。
3. **想起の呼び出し点**: **どの起床クラスで何件を観測に入れるか**。知覚契約書 §3.2 の
   チャネル別枠の表に **記憶の行が無い**(B5 220 tok の内訳は 近接 100/内受容 40/自己状態 65/被注視 15 で満杯)。
4. **想起のクエリ**: `relevance(m|q)` の `q` が何か(現在の観測全文か・起床理由か・目標か)が書かれていない。
5. **埋め込みの計算タイミングと決定論**: 夜間バッチか逐次か。ONNX int8 の**数値再現性**(CPU スレッド数・
   命令セット依存)が `T2 エンジン bit 再現`(運用設計書 §1.3)とどう両立するか。
6. **T2 内省のモデルと呼数**: 「大モデル」(`v2-budget-declaration.md:61` の L5 の見出し)とだけ書かれ、
   **どのモデル・何トークンかが未定**(L2=2,048 tok なら §4 の算術で 78 h/日)。
   艦隊は現在 **7×Qwen3-8B INT8 の 1 種類だけ**(c7-day-4)。
7. **再生時の扱い**: 内省呼をテープの `wake_class` の**どのクラス**で書くか(`EventClass` は 5 値・
   内省用の値が無い)。想起した記憶 ID をテープに書くか(書かないと `prompt_hash` は一致しても
   「なぜその記憶が出たか」が再構成できない)。
8. **関係辺との接続(D-93)**: 「辺の強さ = 相互作用エピソードの基底活性(d=0.5)」にするか。
   `PENDING.md:102` は R-36 の帰還後に C10 §5 を書き換えると宣言。**R-36 は本調査中に帰還した**(§3.3)。
9. **ablation A/B/C の腕定義**: 認知設計書 §7 の A/B/C(推奨/全凍結/persona 可変)と、
   知覚契約書 §8 第1陣⑤(内省 1.05 回/日 vs 2-3 回/日)と、S4 の「**記憶あり/なし 2 腕**」の
   **3 つの ablation が別々に宣言されていて、腕の対応表が無い**。
10. **切替口の形**: `tools/c8/ablations_v1.json` の `AB5-INTROSPECTION` は
    `"kind": "not_implemented"` / `"implemented": false` で、`diff_proposal` に
    「実装時に `run_day(introspection_per_day: float = 1.05)` を口として置く」と書いてある(同 :275)。
    **この引数名と既定値を採るかが未決**。
11. **習慣(T0)との境界**: `PENDING.md:92` D-81 = **K-1〜K-5 を骨子に採用(第215 ユーザー決定)**だが、
    K-1 の「(a) 訪問履歴カウンタのみを土台に / (b)『馴染みの場所』上位 k を観測に書くのは**腕として測ってから**」
    の a/b は未決。K-5(通勤時間の個体分布)は **「C9 の速度と二重に効くので順序を宣言してから」** が条件。
12. **`S(e)` の実装**: 5 項の算出式・θ_imm/θ_cum の値(「予算方程式の解」とだけ決まっている)。
13. **第2陣の holdout と事前登録**: 第1陣の KDDI 5 指標は **09-17 に開封済み・0/4 NOT PASS**
    (`IMPLEMENTED.md` #34)。記憶の合格線は**どこにも無い**。
14. **内省の入力側**: `PENDING.md:119`「設計側第2陣(実装せず記録)」に
    「**日次内省の入力側(expedient 登録済み: 顕著性上位 k+ID 群・T2 入力予算内)**」とあるだけ。
15. **B5 の「気づいたこと」行**: 同 節に「B5『気づいたこと』行(テンプレ v1 の改版=delta+感度試験)」が
    **設計側第2陣の未実装項**として登録されている。記憶の出口候補だが版と感度試験が要る。

### 3.3 本調査中に帰還した R-36 答申が記憶側に足した事実(**親一次確認は未了**)

`docs/research/v2-c10-initial-relations-research.md`(本調査の途中で作業木に現れた未追跡ファイル・
**一次確認 B**・ヘッダ自身が「親一次確認は未了——§『親確認の要請』に 5 件」と申告)。
**関係の答申だが、記憶側の未決を 2 つ新しく出している**:

- **Zhao et al. 2012(CogSci 34th, pp.2603-2608・eScholarship PDF を答申が pymupdf で抽出)
  = 「辺の強さ = ACT-R 基底活性」の唯一の先行**(同答申 §1-6)。逐語:
  「Examining different **memory activation thresholds for links between agents**…」
  「**higher quality relationships are associated with greater cognitive investment and higher memory strength**」。
  規模=40 体・4 ラン・18 標本時点。**閾値 0.0 の網は 800 リンクで平衡(1 体 20 本)・閾値なしは 1,336 リンク**
  (理論上限 1,560)。→ **設計者が置いた数は検索閾値 τ の 1 個だけ**で、Δ も半減期も置いていない。
- **新しく要る未決 2 つ**(同 §1-7 の表): **(A) 検索閾値 τ**(何本までが「思い出せる」か。
  **τ の値が結果を決めている**=0.0 vs なしで 800 vs 1,336 なので感度試験の必須対象。
  「τ をラン中の経験から自己調整する」形は **先行が見つからず=未リサーチ(expedient)**)・
  **(B) エピソードの粒度**(1 発話ブロック=1 本か 1 セッション=1 本か。**数ではなく定義**)。
- **活性値の持ち方**(同 §1-6 の含意 (d)): 原著者が「Activations are not portable or easily interpretable
  in social terms… recasts activations as statements about the **'probability of recall' within a particular
  timeframe**」と書いている → **量子化して持つなら「活性の量子化」でなく「想起確率の量子化」**。
- 好悪(符号)側: **CitySim(Bougie & Watanabe 2025・渋谷の POI 人気と群集密度を照合対象にしている)は
  会話後に positive / neutral / negative の 3 分類を取り出す**(同 §2-1)。一方
  **Li et al. 2026(DASFAA)= 採点規準の並び順を変えただけで Qwen3-8B は 46.22% の点が壊れる**
  (32B 28.56%・GPT-4o <25%)(同 §2-2)→ **8B に数値尺度を書かせる側は危険**。
- 相互作用履歴 → 強さの実証: **Gilbert & Karahalios 2009(2,184 友人関係・70 超の変数)で
  標準化 β 最大は「最終接触からの日数」β=−0.76、2 位が「初回接触からの日数」β=0.755・Adj R²=0.534**
  (同 §1-2)= **ACT-R `A(m)` が計算している量そのもの**、というのが答申の主張。

---

## §4 予算の制約(事実)

### 4.1 バイト(M1 / M4 / M8)

| 項 | 値 | 出所 |
|---|---|---|
| M1 個体状態の総和 | **≤30,000 B/体**(40 万体 ≈12 GB) | `v2-budget-declaration.md:33`。`core/soa.py:73` が実行時に読み `declare` で超えると `ValueError` |
| いま使っている量 | **127 B/体(既定・36 欄)** / 129 B(+plan)/ **142 B(edge+attention+plan・42 欄)** | [再計算] `Registry.budget_report()` を本調査で実行 |
| **M1 の残り** | **29,873 B/体**(既定腕) | [再計算] |
| M4 記憶 | **≤6.1 KB/体**(構造化 3.0 KB + 12 本 × 260 B = 3.1 KB)・**40 万体 2.4 GB** | `v2-budget-declaration.md:36`(delta 2026-09-07)・`v2-cognition-design.md:59` |
| M4 を 390,067 体で | **2.39 GB(2.22 GiB)** | [再計算] 6,120 B × 390,067 |
| M8 RSS 総額 | ≤24 GB。**c7-day-4 実測 1.530 GiB**(`Maximum resident set size 1,604,428 KB`) | `v2-budget-declaration.md:40` / `accept_c7-day-4/c7_accept.md` / `run_time_v.txt` |
| M4 を足した後の RSS[推測] | 1.53 + 2.22 ≈ **3.75 GiB = M8 の 16%** | [再計算・線形加算の仮定] |
| M5 関係(疎表現) | ≤6 KB/体(**宣言だけ・実装ゼロ**) | `v2-budget-declaration.md:37` / `v2-c10-relations-brief.md` §1.1 |
| M9 | **宣言なきフィールドはマージ不可** | `v2-budget-declaration.md:41` |

- **3.0 KB の構造化枠に何件入るか**[再計算・レコード型は未決なので仮定つき]:
  1 件 = 17 B(tick 4+cell 2+相手 4+対象 4+結果 1+重要度 1+種別 1)なら **176 件**、
  1 件 = 24 B なら **125 件**。§2.1 の 8.9 件/体/日 なら **14〜20 シミュ日で満杯**=忘却規則が必須。

### 4.2 呼数と壁時計(L4 / L5 / L7)

c7-day-4 の実測を分母にした算術[全て再計算]:

| 量 | 実測 |
|---|---|
| 呼 | 2,706,593 /シミュ日 = **6.94 呼/体/日**(L4 制御目標 10・上限 400 万) |
| 壁時計 | **33,000.77 s = 9.167 h**(W1 ≤24 h) |
| 艦隊の処理量 | **82.0 呼/s**(= 2,706,593 / 33,000.77)。C7 報告の「78 応答/s(11.2/s/GPU)」と同水準(`docs/ops/build-report-C7.md:63`) |
| `fleet_wait` が壁時計に占める割合 | **89%**(c7-day-3・同 :101)/ c7-day-4 は内訳 20,452.50 ms of 22,917.20 ms/tick = **89.2%** |
| 入力トークン | 3,857,561,181 = **1,425.2 tok/呼**(レンダラの見積り 923.5 tok の **1.54 倍**=見積りは文字数÷2) |
| 出力トークン | 100,549,660 = **37.1 tok/呼**・**3,046.9 tok/s**(`max_tok 96`・o64 既定) |
| 入力トークン率 | **116,893 tok/s**(prefix キャッシュ込み) |

**L5 = 42 万呼/日 を足したときの費用**[再計算]:

| 内省 1 呼の出力 | 合計出力トークン | 追加の壁時計(3,046.9 tok/s で) | 9.167 h に対する比 |
|---|---:|---:|---:|
| **通常呼と同じ(37 tok)** | 15.6 M | **1.42 h**(82.0 呼/s から) | +15% |
| **o64** | 26.9 M | **2.45 h** | +27% |
| **o128** | 53.8 M | **4.90 h** | +53% |
| o512 | 215.0 M | 19.6 h | +214% |
| **L2 = 2,048 tok(認知設計書 §1 の T2)** | **860.2 M** | **78.4 h** | **+855%** |

- 最下行は予算書 L7 の記述と一致する: 「初期値 = **L5 内省 42 万呼 × 2,048 tok ≈ 8.6 億 tok/日**を上限側目安」
  (`v2-budget-declaration.md:63`)。**W1 ≤24 h/シミュ日 を単独で 3 倍以上超える**。
- 入力側: 内省プロンプトを 2,000 tok と置くと 840 M tok → 116,893 tok/s で **2.0 h**[再計算]。
  ただし当日の出来事一覧は**個体ごとに異なる**ので prefix キャッシュが効かない=実効はこれより悪い側[推測]。
- 呼数だけなら余裕がある: 2,706,593 + 420,000 = **3,126,593 = 8.02 呼/体/日**(L4 上限 400 万・目標 10 の内側)[再計算]。
- **L7**: T2 の日次トークン残高が枯れたら「**打ち切りでなく昇格閾値 θ の引き上げ**で守る(生成途中の打ち切り禁止)」。

### 4.3 恒久記録(S1)とテープ

| 量 | 値 |
|---|---|
| S1 上限 | ≤5 GB/シミュ日 |
| c7-day-4 の出力合計 | **0.175 GB**(受入表 S1 行)。c7-day-3 の内訳=`calls.parquet` 177,063,789 B + `blocks.parquet` 84,757 B + `occupancy.npz` 128,892 B(`build-report-C7.md:20`) |
| テープ 1 行の実バイト | **63.1 B/行(zstd 後)** = 177,063,789 / 2,805,670 [再計算] |
| 成長宣言の見積り | `TAPE_ROW_BYTES = 400 B`(`engine/growth_decl.py:52`・**expedient=見積り**)。c7-day-4 の宣言値 1,082,637,200 B / cap 5,000,000,000 B |
| 内省 42 万行の追加 | 宣言ベース **+168 MB**・実バイトベース **+26.5 MB**(応答が同じ長さなら)[再計算]。gist 1-2 文なら実バイトはこれより大きい側 |
| D-R2-6 | 全モジュールが **YAML 5 欄**(`per_agent_bytes`/`per_cell_bytes`/`per_day_growth`/`retention`/`worst_case_ops_per_tick`)で成長を宣言し、**24 step スモークを 1 日・30 日へ外挿して M 行超えで CI 失敗**(`v2-budget-declaration.md:106`・`core/growth.check_growth`) |

### 4.4 埋め込みの CPU

- 件数: **1 体あたり新規 gist 1〜3 本/晩 → 40 万〜120 万 埋め込み/シミュ日**(答申 4-3・`v2-cognition-detail-research.md:327`)。
- ハード(borrowed-2026-09): **Xeon Gold 6226R ×2 = 32C/64T・RAM 251 GB**(`v2-budget-declaration.md:85-86`)。
- **1 文あたりの所要時間は空欄**(未計測・採用条件)。
  逆算だけ示す[再計算]: 120 万本を **16 コア**・ランの 33,000 s 以内に収めるなら
  **1 文あたり ≤440 ms/コア**。32 コアなら ≤880 ms。
- 制約: 「**GPU は LLM 専用**」(認知設計書 §4)・`fleet_wait` が壁時計の 89% なので
  **CPU 側の空き時間はある**が、`movement` の GIL 待ち割合 0.76(c7-day-4)が示すとおり
  同一プロセスに載せると干渉する[推測]→ 実装計画書 :52 の「Proc C 埋め込み worker」が正典。

### 4.5 性能(P4)と決定論

- **P4**: 「全新機能は **ms/シミュ分@基準規模**を宣言し perf 回帰テスト必須。**逐次ループの新設は宣言必須**」。
  → 想起の全走査(per-agent 8-16 件)は **1 起床につき 1 回**のループになる=宣言が要る。
- **P5**: CI 合成負荷ベンチで**前回比 +10% 超の悪化 = ゲート失敗**。
- **T2/T3/T4/T5**(運用設計書 §1.4): エンジン bit 再現・並列不変性(スレッド 1/8/32)・
  規模不変性(n=5,000 と 5,001 で既存個体の乱数列不変)・順序バイアス(|r|≤0.05)。
  → 記憶の書き込み順序が個体 ID に依存すると **T5 に落ちる**。
- 乱数は **Philox(4×64)・鍵=(master_seed, domain)・カウンタ=(tick, entity_id, draw_index)**
  =呼び出し順非依存(運用設計書 §2.5)。`domain_table_version` は manifest の欄。

---

## §5 候補となる決定項(親がアジェンダにする種)

> **すべて「候補」**。値の当否・優先順位は書かない。「既存答申でカバー済み/未リサーチ」を最終列に付す。
> **16 件**(依頼の 10〜15 に対し +1。本調査中に R-36 答申が帰還し「検索閾値 τ」が新しい未決として出たため=M16)。

| # | 何を決めるか | いまの値と出所 | 選択肢(材料から読める形) | 触れる予算・決定項 | 答申の被覆 |
|---|---|---|---|---|---|
| **M1** | **エピソードのレコード型と粒度** | 無し。テープの `response` だけ | (a) エンジン事象 ID + tick + 場所 + 相手 + 対象 + 結果コード + 重要度(**自由文なし**)/ (b) (a)+理由 40 字の逐語 / (c) 呼 1 件 = 1 エピソード(7.01/体/日)/ (d) 「成否が確定した行動」だけ | M4 3.0 KB の構造化枠・`state_hash`・S1 | **既存答申でカバー済み**(答申 4-4「エンジン生成事象は ID を持つので埋め込み不要」・§3-1 の U1) |
| **M2** | **保持数と忘却の規則** | 「12 本」は**統合ベクタ**の数。生エピソードの上限は未定 | (a) 直近 N 件の環状バッファ / (b) `A(m)` の下限で落とす / (c) 1 日 1 回の内省で「構造化のまま残し自由文だけ落とす」(答申 4-5 ③)+(a) | M4・D-R2-6(`per_agent_bytes` の宣言)・P4 | **カバー済み**(ACT-R `A(m)`・d=0.5/0.25・忘却曲線の実測表)/ **件数の上限は未リサーチ** |
| **M3** | **`importance = S(e)` を先に作るか** | `S(e)` は `src/` に **0 件** | (a) S(e) 5 項をエンジンで実装(w1..w5 は expedient)/ (b) 当面 `importance` を結果コード由来の固定表に(失敗>成功 等)/ (c) 重要度を持たず `A(m)` だけで並べる | 認知設計書 §3(θ_imm/θ_cum・昇格率 0.02 回/体/日)・L4 | **カバー済み(式)/ 値は expedient**(感度試験 ±50% が卒業条件) |
| **M4** | **想起の呼び出し点**(どの起床で何件) | 無し。B5 220 tok は近接 100/内受容 40/自己 65/被注視 15 で満杯 | (a) 起床クラスごとに k 件(会話 k=3・計画境界 k=2・個体/セル 0)/ (b) 全起床で一律 k / (c) **内省呼だけ**(想起を観測に出さない) | M11 個体 ≤300 tok(実測平均 133.3・残り 166.7)・§3.2 の等級 E・ablation ①(固定枠 vs 単一ランキング) | **一部カバー**(知覚契約 §3.2 が「固定枠 vs 単一ランキング」を**義務 ablation**にしている)/ **記憶チャネルの枠は未リサーチ** |
| **M5** | **想起のクエリ `q` の定義** | 未定義 | (a) 起床理由 + 現在セル + 直前の結果 / (b) 観測全文 / (c) 「いま何をしますか」の問い | 決定論(`q` が変わると relevance が変わる)・テープ再生 | **未リサーチ**(GA は「query memory」としか書いていない=答申 4-1 の逐語) |
| **M6** | **埋め込みを使うか・どこまで** | 決定は「自由文由来のみ」。実装ゼロ・依存も無い | (a) 第1段は**埋め込み無し**(ID 一致+`A(m)`+importance の 3 項)で出し、relevance は腕として後 / (b) 最初から Ruri-v3-30m を入れる / (c) 会話の要約だけ埋め込む | M4 の 3.1 KB(ベクタ側)・Proc C・**モデル取得の可否**(CLAUDE.md §7 の例外リストに huggingface.co は無い) | **カバー済み(モデル選定・次元・ライセンス)/ スループットは空欄=採用条件** |
| **M7** | **内省の発火口**(D-62 との衝突) | 就寝境界は **LLM を呼ばない**(`run.py:1720-1738`)。`n_reflections` は実質 0 | (a) `plan_sleep` の就寝実行の**直後**に内省呼を 1 本立てる(+ID ハッシュで 0〜30 分ジッタ)/ (b) 行動語「就寝」を LLM に選ばせる形へ戻す(=D-62 の帰無腕)/ (c) 就寝とは独立の「内省起床」クラスを新設 | `WakeCondition`(11 値)・`EventClass`(5 値)・L4/L5・D-56/D-62 の腕・診断列 `reflections` | **カバー済み**(知覚契約 §6「睡眠状態への遷移時+0〜30 分ジッタ+上位 5% の即時内省」) |
| **M8** | **内省 1 呼のモデルと出力トークン** | 未定(「大モデル」とだけ)。艦隊は 7×Qwen3-8B INT8 の 1 種類 | (a) 同じ 8B・o128(+4.90 h)/ (b) 同じ 8B・L2 2,048 tok(**+78.4 h=W1 超過**)/ (c) 別モデル(14B/32B)を少数 GPU に同居 / (d) 出力長を「最大 3 gist × 1-2 文」で機械的に縛る | **W1 ≤24 h**・L5・L7(θ 引き上げで守る)・L6(64 in-flight/GPU) | **一部カバー**(L2=2,048 tok は Rubinstein 由来の **expedient**・ablation {1,024/2,048/4,096})/ **モデル選定は未リサーチ**(品質プローブ④で 8B の recall 0.833) |
| **M9** | **内省の入力の作り方** | `PENDING.md:119`「設計側第2陣」に「顕著性上位 k+ID 群・T2 入力予算内」と**登録だけ** | (a) その日の構造化エピソードを時刻順に全部 / (b) 重要度上位 k 件 / (c) 品質プローブ系統④と同じ **`[B5 記録]` 5 行形** | M11(入力 ≤1,300 tok の契約をこの呼にも課すか)・prefix キャッシュ(個体ごとに異なる=効かない) | **前例あり**(`tools/quality_probe_v0/build_probes.py:508-560` の実測付き文面)/ **本数 k は未リサーチ** |
| **M10** | **gist の出力契約と parser** | 無し。いまの parser は 2 行形(理由/行動・対象・ひと言)専用 | (a) 内省専用の行形(「要旨1: 〈ID 群〉自由文」×最大 3)+専用 parser / (b) 既存の寛容ラベル parser を再利用 / (c) 構造化 1 行「関係: ±1 気分: 語」を**同じ呼で**取る(D-93 (i) の形) | 行動契約 §1 の出力形・書式エラー率の受入 ≤0.10(実測 厳密 0.509 / 実効 0.005)・**指紋の宣言** | **一部カバー**(答申 4-5 の「出力契約 4 項」)/ **行形は未リサーチ** |
| **M11** | **再生に何を書くか** | テープ 13 列。内省用のクラスも想起 ID の列も無い | (a) 内省呼を `EventClass.PLAN_BOUNDARY` で書く / (b) クラスを 1 つ足す(既存 5 値の並びが変わる=`class_rank` の順序規則に触る)/ (c) **想起した記憶 ID を列として足す**(版3)/ (d) 書かない(`prompt_hash` 一致だけで足りるとする) | 運用設計書 §2.5(完全一致・テープ外は失敗)・`shibuya.tape/2` の「末尾に足すだけ」規約・S1 | **カバー済み(規約)/ 記憶固有の列は未リサーチ** |
| **M12** | **D-93: 関係辺の強さを記憶から導くか** | C10 §5 の指紋 3(Δ・半減期 30 日・5 本乱択)は**未リサーチ(expedient)**と宣言済み | (a) 辺の強さ = 相互作用エピソードの `A(m)`(d=0.5)+ 好悪は **`ResultCode` から取る**(断る/すっぽかし=engine 側の事実)/ (b) 好悪は会話応答の 3 値(CitySim 同型)/ (c) 元の指紋 3 で C10 を起動 | M5・C10 の着手条件・「想起確率への正規化」(§3.3) | **R-36 帰還済(§3.3)・親一次確認は未了** |
| **M13** | **習慣(T0)と訪問履歴をこの工程でやるか** | 訪問カウンタは `src/` に 0 件。K-1〜K-5 は骨子として採用済(第215) | (a) 訪問カウンタだけ先に(K-1 (a))/ (b) 「馴染みの場所」上位 k を観測に書く腕まで(K-1 (b))/ (c) T0 習慣スロット表(M3 ≤12.8 KB/体)まで一度に | M3(12.8 KB/体・**未実装**)・M1・D-68 経路 3・D-81 | **カバー済み**(D-81・Lally 2010 の中央値 66 日=「数日では育たない」・U2 の seed) |
| **M14** | **ablation の腕の対応表** | 3 つの ablation が別々に宣言されている | (a) 認知 §7 の A/B/C(推奨/全凍結/persona 可変)・(b) 知覚 §8 第1陣⑤(1.05 vs 2-3 回/日)・(c) S4 の「記憶あり/なし 2 腕 × 7 シミュ日」を**1 枚の表に畳む** | L2(総 GPU 時間の 20% を予約)・`tools/c8/ablations_v1.json` の `AB5-INTROSPECTION`(`implemented: false`)・S4 の 128 h 見積り | **カバー済み(各腕の宣言)/ 対応表は未作成** |
| **M15** | **記憶の合格線(第2陣 holdout)** | **無い**。第1陣の KDDI は 09-17 開封済・0/4 NOT PASS | (a) 内部ゲート(翌日計画の出力長・行動一貫性=知覚契約 §10.3 の「内省効果指標」)/ (b) パターン台帳の holdout 行(A1/A4/Dunbar=C10 R14 と同じ封印)/ (c) D-81 K-2(同一モチーフの再出現が無作為期待の 10 倍以上)・K-3(11〜17 型/被覆 83〜90%)・K-4(Lu 2013 MC(1) 0.87〜0.95 を下限側の門番に) | 事前登録 v2.0・停止則(主張固定)・**複数日ランが要る**(K-2 は 1 日では測れない) | **カバー済み(K-2〜K-4)/ 「記憶の効果」そのものの現実アンカーは空欄** |
| **M16** | **検索閾値 τ**(何本までが「思い出せる/辺である」か) | 無し。**R-36 が新しく出した未決**(§3.3) | (a) 固定 + 感度腕(Zhao 2012 は固定・τ の値が網の大きさを 800 vs 1,336 に変える)/ (b) 毎晩の内省でその体が実際に想起できた数から自己調整(**先行なし=未リサーチ**)/ (c) 閾値を置かず上位 k 件で切る | M4・M5・Dunbar 層(5/15/50/150)・パターン台帳 A1/B3/B4 への較正 | **R-36 でカバー(先行 1 本・Zhao 2012)/ 自己調整は未リサーチ** |

---

## §6 空欄(読めなかった・見つからなかったもの)

1. **`docs/research/v2-memory-retrieval-research.md` の一次確認は丸ごと残っている**: ヘッダ :5 が
   「**一次確認: D = 出典 URL なし(2026-09-02 の規律導入前)**=一次確認が丸ごと残る — 出典痕跡 4 件・**URL 0 件**」。
   U1 の決定行(`v2-redesign.md:392`)の根拠等級も **D**。GA の 0.995/reflection 150・
   AgentSociety/OASIS/Project Sid/AgentTorch の規模・GASim の 316 分・Lally 2010 の 66 日は
   **この答申の記述であって親の一次確認ではない**(R5 答申が GA 原典と Murre&Dros を実読して裏を取った分だけが別)。
2. **ACT-R の原典**: `v2-cognition-detail-research.md:269` が 2 本とも「**書誌のみ・本文未読=空欄**」と申告
   (Springer link / taatgenLebiereAnderson PDF)。同 :495 の未確認一覧にも「ACT-R 論文本文」。
3. **MemoryBank 本文**(AAAI 2024, 38:19724-19731)= **書誌のみ実読・本文未読**(同 :270)。
4. **Lyfe Agents**(arXiv 2310.02172)= 既存答申の出典だが「**本ラウンド未確認**」(同 4-5 の表)。
5. **Sleep-time compute**(arXiv 2504.13171)の数値(テスト時計算 5 倍削減・GSM-Symbolic +13% ほか)=
   「**検索要約のみ・抄録未実読=一次未確認**」(同 4-5 の表)。
6. **Ruri-v3-30m の実機スループット**=未計測(認知設計書 §6 の登録簿・採用条件)。
   **1 文あたりの CPU 時間の根拠が無いので §4.4 は逆算しか書けない**。
7. **c7-day-4 の失敗コード別件数**(`per_result`)が受入記録に印字されていない
   (`tape_stats.json` に列が無い・`run_cli_summary.txt` にも無い)。**「何に失敗した記憶が何件生まれるか」は空欄**。
8. **購入の成立件数**も同様に空欄(売上 19,428,800 円だけが出る。POI ごとに価格が違う(`resolve.py:1553`)ので逆算できない)。
9. **会話 1 セッションの発話本文**は `data/tape/`(gitignore)にしか無い。
   本調査ではテープを開いていない=**「ひと言」欄に実際どんな文が来るか**は空欄。
10. **`EventClass` に内省用の値を足したときの影響範囲**(`class_rank` は繰り延べ優先順そのもの=
    `core/types.py:100-114`「値そのものが class_rank」)を追い切れていない。
11. **ONNX int8 の CPU 決定論**(スレッド数・命令セットで結果が変わるか)についての記述を
    設計書・答申のどこにも見つけられなかった。運用設計書 §1.3 のエンジン bit 再現とどう両立するかは空欄。
12. **「記憶あり/なし」の差を測る指標**(S4 の 2 腕の判定基準)がスケジュール表 :41 の
    「7 日決定論一致・第2陣主張の照合・④⑤ 完走」以上に具体化されていない。
13. **R-36 答申の親一次確認が未了**(答申ヘッダ自身の申告: 本文を機械抽出して逐語確認 8 本・
    WebFetch 経由 4 本・**検索要約のみ 6 本**・**取得不能 2 本**=Zhou et al. 2005 は 403 / IPSS 図中数値)。
    §3.3 の数値(β −0.76 / 0.755・Adj R² 0.534・800 vs 1,336 リンク・46.22%)は**答申の記述**であって
    本書の一次確認ではない。
14. **M3(T0 習慣スロット表 ≤12.8 KB/体)の設計**がどの設計書にも展開されていない
    (予算行と「§10d: 構造的有界・テンプレ intern 共有」の一言だけ)。
15. **C11 の予算 delta 案**(記憶・内省を入れたときの M4/L5/L7/P4/S1 の新しい行)は、
    どの設計書にもまだ書かれていない。S2 の出口ゲートに「予算 delta」とあるだけ(C9・C10 の材料と同じ空欄)。
